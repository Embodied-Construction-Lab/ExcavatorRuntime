# 每日工作汇总：2026-08-26 至 2026-08-28

本文按可核验的 raw Episode、转换 manifest、训练日志、Experiment Run、Git 提交和真机日志
整理。文中的“通过”若未特别说明，均指工程验证，不等同于 ICRA 2027 正式实验结果。

## 8 月 26 日（周三）：双路数据采集、数据集转换与训练启动

### 完成工作

- 在 Orin 上完成两类双 RGB 真机示教采集：
  - `episode_0001`–`episode_0110`：仅挖掘，共 110 次尝试；
  - `episode_0111`–`episode_0145`：挖掘—运转—倾倒，共 35 次尝试。
- 145 次尝试中，139 条为 `complete + success=true`，另有 5 条 aborted、1 条 failed；
  失败和取消目录全部保留，没有为提高成功率而删除 attempted scoop。
- 对 139 条成功 Episode 做独立 validator 检查，139/139 通过。有效数据中未出现串口解析失败、
  命令写失败、传感器无效、相机/磁盘队列丢帧；STM32 sequence gap 原样保留用于审计。
- 将 raw 数据同步至 PC，按任务拆分为两个互不混用的数据集：
  - `dig_only`：104 个 parent Episode、13,007 frame，训练/验证 83/21；训练使用 front RGB；
  - `dig_transport_dump`：35 个 parent Episode、36 个 sequence、7,161 frame，训练/验证
    parent 为 28/7；训练使用 front + dump 双 RGB。
- 仅挖掘数据生成 swing-zero 派生版本，保持四维动作顺序
  `[boom, stick, bucket, swing]`，只把 swing 标签显式置零。
- 启动 seed 2027 的 front-only `dig_only` ACT 400K-step 训练；训练配置、数据 hash、split 和
  checkpoint 间隔被记录。

### 解决的问题

- WebUI 从“固定 200 条 campaign 自动推进”收敛为“一次只采一条 Episode”，支持成功、失败、
  重录和直接采集，避免长时间采集过程中一次故障破坏整批任务。
- 明确两类数据不能混成一个模型：第一模型只学 Dig-and-lift，第二模型学习完整
  Dig–transport–dump 动作。
- 明确 raw 中的 block、zone、dig-point 等字段当时没有可靠填写；后续不使用这些字段推断
  六点位或 pile stage，而使用作者确认的采集计划作为 campaign-level 证据。

### 当日结果与边界

- 双路采集链路具备连续现场工作的基础，数据频率约为：相机 30.2 Hz、STM32 19.6 Hz、
  新状态 9.8 Hz、专家动作 20 Hz。
- 数据适合模型训练和工程测试，但尚不能依靠 raw 标签声称逐 Episode 的六区或料堆阶段分层泛化。

## 8 月 27 日（周四）：训练审计、模型部署、V3-A 冻结与 V3-B 启动

### 完成工作

- 完成 ICRA 2027 Gate 0 工程审计：核验 145 次尝试、139 条有效数据、两类数据隔离、
  parent-level split、训练配置、seed、checkpoint、Git dirty 状态和数据 hash。
- 建立 constant-image state-only ACT 消融数据集：
  - 与视觉基线使用相同 104 个 Episode 和 83/21 split；
  - 保留相同 11D state、4D action 和 ACT 网络结构；
  - 仅把 front 图像替换为固定黑图，不修改源数据和正在运行的训练。
- `dig_only` 模型完成 400K-step 训练，共 800K sample exposure；训练耗时约 21.5 小时，
  速度约 5.1 step/s。
- 建立 checkpoint、deployment manifest、runtime config 和 PC/Orin 模型备份；先发现 380K
  checkpoint 输出轻微越界，随后明确 Runtime 对有限的小幅越界执行 `[-1,1]` 饱和，但
  gross outlier 仍应 fail closed。
- 新训练的仅挖掘模型完成 1/4/7 铲工程真机验证；最新 7 铲任务完成 7/7，整体动作效果良好。
- 将 `act_max_steps` 从 130 调整为 125，使 RL 更早接管后续运料阶段。
- 冻结 V3-A 三仓基线并推送标签 `icra2027-v3a-7scoop-freeze-20260827`：
  - `excavator-il@9b23574`；
  - `excavator-orin-runtime@cf23bb0`；
  - `AiryLidar@ff5a1bb`。
- 创建并统一新阶段名称 `zs/v3-b`，目标是保持 V3-A 可回滚，同时优化切换、训练和完整 ACT
  对照路径。
- 定位训练慢的主要原因：图像作为 JPEG 字节嵌在 Parquet，数据加载约占单相机训练周期 85%、
  双相机约 91%；并非 RTX 5070 Ti 算力不足。
- 建立 provenance-bound image→video 派生训练管线；双相机 200-step 冒烟达到约
  15.38 update/s、61.5 sample/s，无 OOM，loss 有限下降。
- V3-B 首轮取消 Follow 的额外第一帧零命令，将 steady slew 从 `2.0/s` 调为 `3.0/s`，
  保留 terminal-zero ACK、generation/authority 和故障归零边界。

### 解决的问题

- 训练脚本支持 `dig/full/both` 独立执行、image/video 选择、preflight 和受 provenance 约束的
  resume，避免第一个模型训练完成后无法单独重启第二个模型。
- 明确 batch 增大后应按 sample exposure 缩短 update 数，避免以“step 数相同”为名把训练预算翻倍。
- 将“当前工程数据可训练”与“论文 R01/R02 已关闭”分开：单次、多铲成功、训练 loss 和工程模型
  都不能替代 held-out、多 seed 和正式统计实验。

### 当日结果与边界

- V3-A 成为已冻结可回滚基线，V3-B 成为开发分支。
- V3-B 首轮 handoff 优化只通过 Host 测试；当天尚未完成新的发动机关闭 HIL 与分级真机验收。

## 8 月 28 日（周五）：高吞吐双相机训练、完整 ACT 并行入口与双斜率切换

### 完成工作

- 高吞吐 video 管线完成 `dig_transport_dump` 双 RGB ACT 正式训练：batch 4、150K update、
  600K sample exposure，约 2 小时 25 分完成，稳定速度约 17 update/s。
- 完成 validation shortlist，工程候选选择 115K checkpoint：
  - validation deployment-prior L1 为 0.1138；
  - 输出全部有限，范围约 `[-1.0365, 1.0452]`；
  - 277 个值需要正常化饱和，但没有超过 1.25 的 gross outlier。
- 为完整 ACT 模型建立独立配置与入口，使其与现有
  “ACT 挖掘 + RL 运料 + Fixed Dump + RL 返回”主线并行，不替换已验证路线。
- 修复 V3-B 首轮启动中的 owner readiness、固定循环 plan/状态字段契约等问题，并完成 4 铲
  真机闭环。
- 分析 4 铲日志：ACT→RL 从旧策略终态零 ACK 到 RL 第一条非零命令 ACK 平均约 160.5 ms；
  从终态零 ACK 到铲尖首次移动 5 mm 平均约 427 ms。主要停顿不再是模型冷启动，而是安全
  交接、RL 首段斜率和液压建立运动共同造成。
- 实现 RL Follow 分轴双斜率：
  - 首次从零接近非零目标：`4.0/s`；
  - 正常跟随、减速和反向：`3.0/s`；
  - 急停、取消、异常和终态归零仍立即执行，不经过斜率。
- 双斜率代码和活动配置已同步 Orin；PC 全量 Orin Runtime 296 项、Orin 本机 291 项测试通过。
- 将 ACT→RL 故障、耗时分解、处理方案和验收顺序写入诊断笔记。

### 解决的问题

- 将“控制权交接耗时”和“肉眼看到机械动作的耗时”拆开，避免继续误判为模型加载慢。
- 不采用把死区内动作直接抬到死区下限的跳变方案，也不提前让两个策略同时持有运动权；
  通过受控双斜率改善启动，同时保留可审计的单一 authority。
- 完整 ACT 模型的轻微越界与异常失控输出分开处理：正常小幅越界饱和，非有限值和 gross
  outlier 仍拒绝。

### 当日结果与边界

- 双相机完整动作模型已有离线工程候选，但完整 ACT 真机链路尚未按 Shadow→发动机关闭→
  单铲/多铲顺序验收，不能替换主线。
- `4.0/3.0` 双斜率已完成代码和 Host 验证，尚需发动机关闭 HIL 和 1/4/7 铲真机复测。
- V3-B 三仓仍有未提交工作，正式交付前需要完成审查、提交、push 和新的基线记录。
