# 诊断笔记：从无运动到根因

诊断原则：先只读、再零命令、最后才允许运动；一次只启动一个硬件 Owner。保留完整日志，不通过放宽 timeout 或 freshness 门限掩盖问题。

## 1. 固定诊断顺序

### 1.1 网络和配置

```bash
ping -c 5 192.168.50.2
ssh -o BatchMode=yes -o ConnectTimeout=5 jetson16@192.168.50.2 'hostname; date --iso-8601=seconds'

cd /home/zhaoshuai/workspace_uinty/RL_prj/excavator-il
conda activate excavator-il
python scripts/check_site_config.py
```

### 1.2 资源 Owner

在 Orin：

```bash
fuser -v /dev/ttyTHS1 /dev/video0 /dev/video2 2>&1 || true
pgrep -af '[o]rin_state_sender|[r]esident_act_runtime|[e]xcavator-il collect' || true
```

未知 PID 先查看完整命令和启动者，不直接全局杀进程。

### 1.3 STM32 只读链路

```bash
python scripts/diagnose_stm32_link.py
```

通过标准：`passed=true`、解析失败 0、序号间断 0、18～22 Hz、最大接收周期不超过 80 ms。

### 1.4 零命令 soak

发动机关闭、双杆回中、deadman 释放：

```bash
python scripts/run_zero_command_soak.py
```

通过标准：非零命令/回显/有效动作均为 0；STM32 约 20 Hz，新状态约 10 Hz，两路相机约 30 Hz。

## 2. 现象 → 根因 → 处理

| 现象/错误 | 常见根因 | 处理 |
|---|---|---|
| STM32 `must be ASCII`、字段不是 55、NUL/乱码 | 充电器导致链路断开，或串口电气状态异常 | 拔掉充电器，重启 STM32/Orin 后先跑只读诊断 |
| `serial is already owned` / `Could not exclusively lock` | RL、Collector 或 ACT 重复启动 | 用 `fuser`/`pgrep` 找具体 Owner，通过原 UI/终端正常停止 |
| `sensor_valid=false`、新状态 0 Hz | STM32 未复位、传感器链路失效或仍在充电 | 检查供电/充电器、复位 STM32，再诊断 |
| `/joint_states` 无数据 | Orin 状态流、UDP 18081 或 PC bridge 未运行 | 查 Orin Runtime、端口和 Operator profile |
| JointState 有但 RViz 某关节不动 | 映射、单位、关节名或 URDF/TF 问题 | 对照 Web UI 原始角度与 `/joint_states`，逐关节检查 |
| `local_map.timestamp_s 已过期或来自未来` | PC/Orin 时间不同步，或 LocalMap 没有新数据 | 检查 Chrony/systemd-timesyncd，再查感知栈 |
| `live planning inputs did not become fresh` | AiryLidar Operator/雷达/地图没启动或时间戳失效 | 启动 `live_commissioning`，逐级查点云→地图→状态 |
| UI 无相机，显示“等待 Collector” | 未启动 Collector 时属正常；运行中则是 18092/设备 Owner 问题 | 查 `/healthz`、相机 Owner 和 by-path 设备 |
| `docker: bad format for path ...by-path...:/dev/video0` | USB by-path 名含冒号，直接传给 Docker 解析歧义 | 当前脚本已先 `readlink -e` 为 `/dev/videoN`；确认 Orin 代码已同步 |
| import-only Docker 检查提示 `NVIDIA Driver was not detected` | 命令未传 GPU，仅做 Python import | 属正常；真推理用项目 launcher 并运行 GPU 诊断 |
| ACT `Found no NVIDIA driver` | 真推理容器未注入 NVIDIA Runtime | `bash scripts/diagnose_orin_gpu_access.sh`，检查 Docker runtime |
| 安全停止后又报 `terminal disarm ... request failed` | 旧版本重复发送 terminal disarm，第一次已关闭 socket | 更新到含幂等 disarm 的版本；最新回归已通过 |
| ACT 完成后 `ACT-to-RL handoff was revoked` | 已发现两条独立路径：旧版 status 混合快照；目标模式零回显未等完整安全门就提前激活，随后被同一帧撤权 | 同时更新 Orin 原子 status 与 target-mode safety claim 修复；不要放宽安全门或仅增大 timeout |
| Orin Git push 连接 `192.168.31.219:7897` 失败 | Orin 仍配置旧 Wi-Fi 局域网代理 | 项目同步走 PC push + 有线 bundle；需要外网时再单独修代理 |
| GitHub/SSH 能 ping 但 SSH 旧地址断开 | Orin Wi-Fi 地址变化 | 使用稳定有线地址 `192.168.50.2` |

## 3. 相机专项

```bash
ssh jetson16@192.168.50.2 'ls -l /dev/v4l/by-path/; v4l2-ctl --list-devices'
curl -fsS http://192.168.50.2:18092/healthz
```

正式采集前使用项目双相机预检脚本；通过自动指标后仍要人工查看 UI 两幅画面，确认倾倒相机没有红/黑遮挡。

## 4. ROS/规划专项

```bash
source /opt/ros/jazzy/setup.zsh
source /home/zhaoshuai/workspace_uinty/RL_prj/AiryLidar/ros2_ws/install/setup.zsh
ros2 topic hz /joint_states
ros2 topic hz /localmap/machine_root_ros_points
ros2 topic echo /bucket_tip_pose_machine_root_ros --once
```

顺序必须是：`machine_state_v1 → /joint_states → TF/FK → bucket tip → LocalMap → Plan`。前一级不成立时不要继续调后一级。

## 5. 日志位置

| 内容 | 位置 |
|---|---|
| UI/Guided/Hybrid | PC `excavator-il/logs/` |
| ROS | PC `~/.ros/log/latest/` |
| 感知与 LocalMap | PC `AiryLidar/runtime/logs/`、`localmap/exports/live_latest/` |
| Orin RL | `excavator-orin-runtime/deploy/logs/` |
| ACT | Orin `/home/jetson16/workspace_excavator/act_inference/logs/` |
| 原始 Episode | Orin `/home/jetson16/workspace_excavator/data/excavator-data/` |

一次故障至少记录：北京时间、任务阶段、目标点、PC/Orin commit、配置 SHA、完整错误、现场视频和硬件状态。

## 6. 已处理故障记录

### 6.1 2026-08-24：ACT 结束时的 control status 混合快照

**现象**

- 自动多铲的第一次 ACT 挖掘完成后停止，未进入 RL 倾倒。
- UI 报 `RuntimeError: ACT-to-RL handoff was revoked`。
- 同期日志显示 `prepared follow ready`、`sensor_valid=True`，ACT worker 也未提前断开。
- 随后的 `resident motion core disarmed` 是 PC 检测到错误后的正常安全清理，不是首发原因。

**根因**

Orin 的 `resident_motion_control.v1` status 旧实现先读
motion authority，再单独读 ACT segment。ACT 最后一条命令 ACK
正好在两次读取之间到达时，一个 status 会同时包含：

- 交接前：`active=act_dig` 且 `target=null`；
- 交接后：`act_segment_complete=true` 且 `act_is_active=false`。

这个组合在任何真实时刻都不存在，但 PC 会将它按真实撤销处理。
因此故障具有偶发性：同一套代码可能成功运行三铲，也可能在某次
ACT→RL 切换时触发。

**解决方法**

- Orin `ResidentMotionCore` 新增单一原子 control status 快照。
- motion authority、ACT segment、RL/ACT active、Mission lease 和
  operational 状态全部在同一把 control lock 内读取。
- 保留 PC 对真实交接撤销的 fail-closed 判断；不增大 timeout，不放宽
  传感器或串口 ACK 门禁。
- 增加确定性并发回归：强制最后 ACK 落在两次旧读取之间，旧版
  稳定复现现场矛盾 status，修复后通过。

**后续结论**

这是真实竞态，但 13:33 使用原子 status 版本仍复现相同 UI 错误，
说明它不是全部根因。该修复应保留；不能再把它单独视为现场问题已经闭环。

### 6.2 2026-08-24：目标 RL 零回显安全未就绪时被提前激活

**现象与证据**

- ACT 130 steps 正常完成，prepared DUMP trajectory 已就绪。
- Orin 没有串口 timeout、CSV parse failure、Mission lease expiry 或 ACT
  worker 提前断连记录。
- 失败只发生在 ACT 最后一条命令 ACK 后的 ACT→RL 零命令交接窗口。
- 公共 `ResidentCommandSink` 回归可稳定复现同一个 authority 结果：
  目标 velocity-reference 零命令的序号、模式和动作都匹配，但
  `control_enabled=false` 时，旧实现先进入 active RL，随后立即安全撤权，
  generation 再次增加且 `target=null`；PC 因而报告
  `ACT-to-RL handoff was revoked`。

**根因**

旧 `_ack_matches_locked()` 把目标模式零回显只当作“命令 ACK”验证，
没有把 `control_enabled`、`estop`、`sensor_valid`、`stm32_alive`、
`fault_flags`、`command_valid/timed_out` 和目标 control mode 作为一个
完整的目标策略激活门。目标模式刚切换时若第一帧安全状态尚未收敛，
就会出现“先 active，再被同一帧 fail-closed 撤权”的瞬态。

**解决方法**

- terminal source zero 之后继续保持零命令和 `target_zero_pending`。
- 目标零回显只有在完整 safety predicate 同时成立时才 ACK 并激活 RL。
- 未就绪帧不激活任何策略、不发送非零命令，也不撤销交接代次；下一帧
  安全回显可完成同一交接。
- PC 撤权异常增加原子 status 摘要：phase、generation、active/target、
  ACT segment steps、RL/ACT active 和 Mission lease，便于后续直接定位。
- 新增回归覆盖 unsafe target zero → 保持 pending → safe target zero →
  active RL 的完整时序。

**当前状态**

已于 2026-08-24 13:45 完成真机自动 3 铲复测：3 次 ACT→RL
均进入 Follow，任务最终输出“装车循环完成：3/3”，无
`ACT-to-RL handoff was revoked`。终态零命令得到 STM32
回显确认，串口、ACT data link 与 control socket 正常释放。
本故障的现场功能闭环已完成。

**复测顺序**

1. 同步新版 `excavator-orin-runtime` 后确认串口/相机无 Owner 残留。
2. Web UI “开始分阶段验证”：RL 到挖点 → ACT 挖掘 → RL 倾倒。
3. 自动 1 铲。
4. 自动 3 铲，确认每次 ACT→RL 都进入 Follow，无
   `ACT-to-RL handoff was revoked`。

### 6.3 2026-08-24：3 铲成功后仍存在的 ACT→RL 停顿

**现象与定量证据**

- 同一次 3 铲 Mission 生成 6 条 STM32 ACK 级别的
  `resident_handoff_sample.v1`，所有交接均成功。
- RL→ACT 三次为 203.5 ms、146.5 ms、214.7 ms，均值 188.2 ms。
- ACT→RL 三次为 602.4 ms、3942.6 ms、4217.7 ms。
- 后两次的零命令换权分别仅 102.2 ms 和 46.9 ms，STM32
  首条非零回显分别仅 46.8 ms 和 49.0 ms；主要时间消耗在
  `policy_ready_wait_ms=3793.7/4121.8`。
- 第二铲 prepared trajectory 因“首 waypoint 与实时铲尖距离
  0.2384 m 过大”安全降级到普通 Plan→Follow；第三铲 prepared planner
  在尚未 ready 时被取消，同样走了普通路径。

**结论**

当前已消除模型/进程冷启动，并完成了可靠的常驻双策略换权；
但 ACT→RL 的流畅性仍受 prepared trajectory 可用性限制。接下来不应
放宽 safety gate，而应让 dump 轨迹在 ACT 运行中更靠近交接时刻刷新，
并记录 prepared 失效/取消的确切原因。本次只有 3 个每方向样本，
可用于功能验收，不能作为论文延迟统计。

### 6.4 2026-08-24：7 铲复测中 ACT→RL 再次变慢，末次 RL 返回不收敛

**现场日志**

- Mission 基名：`hybrid_mission_20260824_143007`。
- 7 次 RL→ACT 交接延迟为 147.8–254.2 ms，均值 192.8 ms；该方向正常。
- 7 次 ACT→RL 交接延迟为 7902.1、8070.2、4728.2、6066.9、
  621.0、6315.3、4772.5 ms，均值 5496.6 ms、中位数 6066.9 ms。
- ACT→RL 的目标零命令确认和首条非零 STM32 ACK 仍分别约 49–114 ms
  和 44–52 ms；平均 `policy_ready_wait_ms=5375.4`，因此慢点不在串口、
  常驻 ACT worker 或 motion authority 原子换权。
- prepared DUMP 共 7 次尝试：6 次生成 ready trajectory，其中 5 次因首
  waypoint 与交接时新鲜铲尖距离为 0.2692–0.4088 m 而安全回退，1 次直接
  Follow 成功；另 1 次尚在规划时被取消。实际复用率仅 1/7。
- 最后一次 RL 返回 `dig_02` 是另一项独立异常：Follow 连续输出 790 个
  action datagram，始终停留在 waypoint 1/3；距离从 0.3538 m 增大并长期
  停在约 0.642 m，最终 Mission 终止后报告 `MOTION_GATE_CLOSED`。
  全程 STM32 `sensor_valid=True`，所以不能把这段约 40 秒停留解释为策略
  冷启动或 ACT→RL 换权等待。

**已确认根因：prepared 起点过早冻结**

当前 `prepared_dump_lead_steps=20`，ACT 以 10 Hz 执行，因此 DUMP 规划在
ACT 结束前约 2 秒冻结实时铲尖状态。ACT 在余下 20 steps 中仍会显著改变
铲尖位置，导致交接时预规划轨迹首点已落后于真实状态。活动配置的
`prepared_start_tolerance_m=0.15` 正确阻止了不连续轨迹直接执行；不应把
阈值放宽到 0.3–0.5 m 来换取表面流畅。

**解决方案与验收计划**

1. 保留 0.15 m 激活安全门和 STM32 ACK 级换权，不修改安全语义。
2. 将“规划进程预热”和“冻结规划起点”分离：ACT 早期只让 planner/service
   常驻；临近 ACT 终点时再用最新铲尖状态生成 trajectory。若一次规划无法
   在剩余窗口完成，应显式报告 fallback，不静默把重新规划时间算成策略换权。
3. 先用 5/10/15 个 lead steps 做发动机关闭或受控真机对比，记录每次规划
   用时、交接首点偏差、prepared 是否直接激活和 ACK 级 handoff；选择满足
   规划完成率且首点偏差稳定小于 0.15 m 的最晚触发点，而不是直接猜参数。
4. 为 RL Follow 增加低频、结构化诊断证据：目标 waypoint、实际铲尖、归一化
   策略输出、转换后的物理速度、STM32 回显动作及死区/限位裁剪原因。再次出现
   “datagram 增长但距离不收敛”时，才能区分输出方向错误、动作落入死区、
   坐标契约不一致或液压执行未响应。
5. 回归验收至少连续 7 铲：prepared 直接激活率不低于 90%；ACT→RL 每次
   `latency_ms < 1000`，目标仍是 p50 < 200 ms、p95 < 300 ms；任一 RL
   waypoint 连续 2 秒距离无至少 0.01 m 的下降时，立即输出明确 stall
   诊断。第一轮只增加证据、不改变真机运动语义，仍由现有 tracking timeout
   负责有界终止；取得动作与距离证据后再决定是否需要更早 fail-closed。

**当前状态**

已在 `zs/act-v2` 实现“两次冻结”而不是放宽阈值：ACT 余 20 steps 时完成
第一次 Plan 热身，余 5 steps 时通过同一个常驻 ROS 进程刷新 live inputs 和
trajectory；激活信号与刷新信号同时到达时，刷新具有更高优先级，避免误用旧
trajectory。日志新增每次 `planning_ms`、激活时 `frozen_input_age_ms` 和
`first_waypoint_distance_m`。

Orin RL Follow 也已增加低频结构化 `RL Follow no progress` 警告：同一
waypoint 在 2 秒内没有至少 0.01 m 的距离下降时，记录原始归一化动作、slew
后的命令、物理速度和 controller backend；不会改变动作或缩短原有 timeout。

上述实现的软件回归已通过，现场效果仍为 `Needs runtime verification`。
下一次先执行分阶段单铲，再执行自动 3 铲；重点确认 refreshed trajectory
直接激活、ACT→RL ACK 延迟，以及是否出现 no-progress 警告。本次 7 铲运行
不能作为“无缝切换”论文结果，只能作为失败/诊断样本保留。

**第二个已确认的延迟源：OctoMap QoS 不匹配**

- `octomap_server` 现场日志明确记录
  `Publishing latched`，即 `/occupied_cells_vis_array` 是可向迟到订阅者重放
  最新样本的 `TRANSIENT_LOCAL` 话题。
- 旧的一次性 obstacle exporter 使用 `create_subscription(..., 10)`，
  实际是 `VOLATILE` 订阅；它不会收到已缓存的最新 MarkerArray，
  只能等待下一次地图发布。这与日志中 4–8 s 的 `policy_ready_wait_ms`
  以及配置中 `octomap_timeout_s=5.0` 相符。
- 修复后 exporter 显式使用 `RELIABLE + TRANSIENT_LOCAL + depth=1`，
  与发布端的 latched 语义对齐。规划仍使用真实 OctoMap 障碍物，
  没有跳过碰撞检查，只是消除等待“下一帧”的不必要停顿。
- 下次真机日志应同时出现 `prepared follow refreshed`、
  `planning_ms`、`frozen_input_age_ms` 和 `first_waypoint_distance_m`。
  只有 ACK 级 `ACT→RL latency_ms < 1000` 才算通过；若仍超时，
  使用这四项证据区分“规划未完成”和“起点又过期”，不再猜测。

### 6.5 2026-08-24：分阶段验证在 RL 到挖点后停住

**现象与证据**

- Mission `hybrid_mission_20260824_154021` 中 RL Plan→Follow 成功，
  日志明确输出 `混合 Mission 分段完成：rl_to_dig`。
- 随后约 101 秒内 Orin 仍以正常速率输出状态，
  `sensor_valid=True`；无 serial timeout、CSV parse failure、安全门撤权
  或 ACT worker 断连。
- 本次没有 `activate_act`、ACT segment generation，也没有任何
  RL→ACT `resident_handoff_sample.v1`。因此不是“发起换权后失败”，
  而是 PC 编排层没有收到进入下一段的命令。

**结论与正确操作**

`开始分阶段验证` 故意只执行当前一段：第一次只执行
RL 到挖点，完成后 UI 进入 `awaiting_act_dig`。操作员需要再点击
`执行 ACT 挖掘`；ACT 完成后再点击 `前往倾倒并倾倒`，最后点击
`RL 返回挖点`。需要自动连续切换时，应使用 `自动装车 1 铲`，
而不是分阶段入口。

**充电期间的验证边界**

挖掘机连接充电器时，现场硬件可能断开 STM32 与挖掘机之间的
传感器/执行链路。充电时的 `sensor_valid=False`、零新状态率或
`control_enabled=False` 不得用来判定策略切换性能。充电完成后必须：

1. 拔掉充电器；
2. 复位 STM32；
3. 先运行 `python scripts/diagnose_stm32_link.py`，确认 `passed=true`；
4. 再进行分阶段或自动 Mission 真机验收。

### 6.6 2026-08-24：Orin NX 16GB 只识别约 8GB

**现象与已确认证据**

- `/proc/meminfo` 显示 `MemTotal: 7727768 kB`，`tegrastats` 显示
  `RAM .../7547MB`；这不是 16GB 设备的正常保留内存差异。
- `/etc/nv_boot_control.conf` 的 `TNSPEC=3767-303-0000-...`，其中
  P3767-0000 是 Orin NX 16GB SKU；运行期模型同样声明
  `p3767-0000`。
- 当前 `/proc/device-tree/memory@60000000/reg` 声明的物理内存大小是
  `0x400000000`，即 16GiB；`/proc/cmdline` 中不存在 `mem=8G`
  之类的 Linux 限制参数。
- 内核日志却只收到 `8050304K` 总内存，说明 16GiB 的 DT
  声明并没有变成可供 Linux 使用的完整内存映射。
- `/sys/devices/system/cpu/{possible,present,online}` 都显示 `0-7`，
  `lscpu` 也确认 8 核；因此 CPU SKU 识别正常，问题集中在
  DRAM/固件内存映射，不应误判为整个模组被当成 8GB SKU。
- 手工把 `extlinux.conf` 改为 p3767-0000 的 16GB DTB 并重启后
  问题仍在，因此不是 Linux DTB 选择或内核参数问题。

**结论**

当前 QSPI 内的 MB1/MB2/UEFI/BCT 启动固件很可能含有错误的
DRAM 初始化/内存布局。DRAM 初始化发生在 Linux 之前，
所以不得用 `mem=16G` 或继续修改 `extlinux.conf` 强行解决。

**修复路径**

1. 使用 Seeed 针对 **reComputer Classic J401 + Orin NX 16GB +
   JetPack 7.2/L4T 39.2** 的官方 BSP，不得使用新款
   reComputer Super 的镜像。
2. 下载后必须校验官方 SHA256
   `bbaae572ddcfb745a9ce519ade817bf238a9cd9be8c0177bbef6ada353f89878`。
3. 先备份 Orin 用户目录、未提交 Git 工作树、模型/数据和
   `excavator-act-inference:jp72-pytorch261` 镜像。
4. 在 Force Recovery 模式下先用 USB ID `0955:7323` 再次确认
   物理模组为 NX 16GB。
5. 优先使用已确认的 Seeed 板级配置只更新 QSPI，保留 NVMe；
   若官方包无法证明 QSPI-only 不会触及 NVMe，则只能在完整
   备份后执行 Seeed 的 NVMe 全量刷写。
6. 修复后验收：`lscpu` 为 8 CPU，`free -h` 约 15GiB，启动
   TNSPEC 仍为 P3767-0000，然后再回归 Docker GPU、STM32、双相机和
   ACT/RL shadow。

**附加安全发现**

当前 `nvpmodel` 是 40W，而 Seeed 官方对 Classic J401 明确警告
Orin NX 16GB/8GB 不要启用 MAXN Super，否则散热能力不足。
内存修复后应切回该载板支持的 20W/标准模式，并重做温度与
ACT 推理频率验收。

**2026-08-24 修复准备状态**

- Seeed Classic J401 / Orin NX 16GB / JP 7.2 官方镜像已下载到 PC，
  文件大小 `13458228213` bytes；实测 SHA256 与官方值完全一致。
- PC 已备份 Orin 的用户目录、系统配置和本次 ACT Docker 镜像；
  Docker 归档 SHA256 为
  `f7eefaa40a69a336933ffa96234b713c9eb583b0bd809e09be1502cf8af9d033`。
- 官方 massflash 包默认同时包含 QSPI 与 NVMe 镜像，不能直接运行默认
  flash 命令。已在解包副本中启用 NVIDIA 刷写器原生的
  `--qspi-only` 路径；最终下发到目标端的命令必须显式包含该参数。
- NVMe 上的下一次启动配置已准备为 Classic J401 + P3767-0000 的
  `recomputer.dtb`，并保留当前 Super 16GB 与原始 8GB 两个菜单回退项。
- 真正刷写前仍必须完成三项人工门禁：安装 PC 依赖、关机进入 Force
  Recovery、由 `lsusb` 确认唯一设备为 `0955:7323`。任何一项不符都停止。

**2026-08-24 QSPI-only 修复结果**

- 第一次运行在 `SSH ready` 后停住。PC 进程仍在，但 Orin initrd 内实际
  阻塞于 `mount.nfs`，处于 `rpc_wait_bit_killable`，尚未启动
  `l4t_flash_from_kernel`，因此当次没有擦写 QSPI，也没有触碰 NVMe。
- 根因是活动 UFW 只临时开放了 NFS 2049；initrd 使用 NFSv3，还需要
  rpcbind 111 和动态 mountd 端口。刷写包装脚本现会在运行期额外仅对
  USB initrd 网段 `fc00:1:1::/48` 放行入站流量，并在退出时自动删除。
- 第二次日志明确出现 `Starting to flash the QSPI`、各分区 SHA1 校验和
  写入，最终输出 `Successfully flashed the QSPI`、`Flash is successful`；
  临时 UFW 规则也均成功删除。
- 重启后 USB ID 为 `0955:7020 L4T running`，系统仍从
  `/dev/nvme0n1p1` 启动，证明 QSPI-only 路径保留了原 NVMe 根文件系统。
- 最终验收：内核 `6.8.12-1021-tegra`，设备模型为
  `NVIDIA Jetson Orin NX Seeed recomputer classic Super`，
  `/proc/meminfo` 的 `MemTotal=15973152 kB`，`free -h` 显示约 15GiB。
  16GB 硬件只识别约 8GB 的问题已修复。

### 6.7 2026-08-24：QSPI 修复后重接真机的设备验收

**已通过**

- PC 直连网卡为 `192.168.50.1/24`，Orin `192.168.50.2` 连续
  ping 延迟约 0.2–0.5 ms，SSH 正常。
- Orin 仍从 `/dev/nvme0n1p1` 启动，`MemTotal` 约 15GiB；PC 与
  Orin 时间同步，`NTPSynchronized=yes`。
- GPU 设备节点、Docker 和 `excavator-act-inference:jp72-pytorch261`
  存在；容器内 `torch.cuda.is_available()` 为 true，显存约 15.23GiB。
- front/dump 两个 `32e6:9251` UVC 设备都在线，固定路径分别
  解析到 `/dev/video0` 和 `/dev/video2`；两路均为 640×480、30.2 FPS，
  自动双相机诊断 `passed=true`。

**STM32 串口复测结论：已通过**

- `/dev/ttyTHS1` 存在且无残留进程占用，配置为 460800 8N1。
- 连续两次 `diagnose_stm32_link.py` 均失败：控制序列速率仍
  约 19.3–19.6 Hz，但每个 10 s 窗口出现 11–15 条损坏帧，包括
  NUL、非 ASCII、schema 单字节翻转、数值位翻转和字段丢失；
  最大接收周期 106–115 ms。
- TX/RX 完全接反不会产生这种“大部分可解析、少量位翻转”
  现象；当前首先检查 Orin RX↔STM32 TX 插头接触、共地 GND
  及线缆与动力线的距离。未复测到 `passed=true` 前，不得启动
  Collector、RL、ACT 或真机运动。
- 重新上电、重插后的第三次复测曾仍有 5 条损坏帧和 1 次序列中断。
  Classic 与 Super DTB 的 `serial@3100000` 节点完全一致，均为
  `nvidia,tegra194-hsuart`、同一 clock/DMA 引用；当前 IMX477 overlay
  也没有触及 UART。因此已排除“Super/CSI DTB 直接改坏 UART
  节点”这一假设。
- 调整现场 TX/RX/GND 接线后连续三次 10 秒诊断均通过：每次约
  194–195 帧、解析失败 0、控制序列间断 0、速率约 19.32–19.34 Hz，
  最大接收周期约 61–63 ms。此前故障最终归因于物理接线/接触质量，
  不是 QSPI 修复或 UART 设备树配置。

**尚未通过：dump 相机机械安装**

- dump 设备和数据链路在线，但现场 JPEG 横置约 90°，左侧被
  橙色机身大幅遮挡，画面中还有线缆穿过。这不是 UVC 映射
  错误，而是相机朝向/安装位需调整。正式双相机采集前必须使
  倾倒区和卡车成为主视野，校正画面方向并移除遮挡，然后重跑
  `diagnose_dual_camera.py` 并人工检查两张 JPEG。

### 6.8 2026-08-24：双相机改造后标准 ACT Shadow 启动失败

**故障一：主机相机路径与容器相机路径被错误比较**

- 现象：`run_act_shadow.sh` 进入容器后立即报
  `ACT and operator observation cameras must match`。
- 根因：采集配置用稳定的主机路径 `/dev/v4l/by-path/...`，ACT 配置用
  容器内路径 `/dev/video0`。二者指向同一前视相机，但属于不同设备
  namespace，不能按字符串相等判断。
- 修复：标准 Shadow/Motion 启动器均从 `collection.orin.json` 读取
  `camera_front.device`，解析真实字符设备后显式映射为容器
  `/dev/video0`；Runtime 继续严格校验串口和相机分辨率/FPS，但不比较
  跨 namespace 的设备字符串。

**故障二：LeRobot 模型加载依赖隐含 import 顺序**

- 现象：越过相机检查后，模型加载报
  `AttributeError: module 'packaging' has no attribute 'version'`。
- 根因：LeRobot 通过 `packaging.version` 包属性访问版本 API，而当前
  `packaging` 不会自动加载该子模块；Dig Policy 惰性加载后，过去由
  其他模块偶然完成的隐式导入不再存在。
- 修复：只在 commissioned LeRobot ACT backend 的加载边界显式导入
  `packaging.version`，保持不使用 LeRobot 的策略 seam 仍可惰性加载。

**修复后验收**

- ACT Shadow 连续运行 330 steps，推理频率 `10.0007 Hz`；
- `dropped_state_count=0`，最大 state-to-decision `69.5 ms`，最大相机
  age `42.0 ms`；
- `serial_write_count=0`、`nonzero_serial_write_count=0`；
- 退出后 `/dev/ttyTHS1`、`/dev/video0`、`/dev/video2` 均无残留 owner。

### 6.9 2026-08-24：ACT→RL 倾倒阶段突然报 `MOTION_GATE_CLOSED`

**现象**

- ACT 挖掘完成后已经进入预生成的 DUMP Follow，挖机开始向倾倒方向运动，
  随后轨迹中断并报 `Follow failed: MOTION_GATE_CLOSED`。
- WebUI 很快把 Mission 标成“失败”，原实现同时禁用了“安全停止”按钮；
  操作者无法在失败后的清理窗口再次主动请求停止。
- Orin 最终日志显示 resident motion core 主动 disarm、终态零命令已确认，
  owner、ACT worker、串口和相机均已释放，因此本次没有残留运动源。

**已确认证据与根因**

- 故障前 STM32 状态仍为 `sensor_valid=True`，约 20 Hz 状态持续更新，
  RL Follow 已收到 44 个动作数据报；不是串口、传感器或 ONNX 推理失败。
- Orin 的 RL 远程执行门只会在 resident RL authority 或 Mission lease
  失效时返回该错误。本次 PC 使用新的 SSH 进程每 0.4 秒续租，而默认
  Mission lease 只有 1500 ms；一次 SSH 续租延迟超过该窗口后，Orin
  按 fail-closed 设计关闭运动门并终止 owner。
- 故障前 ACT→RL 的实测首个非零命令间隔约 1519 ms，其中约 1373 ms
  是等待目标策略就绪；这进一步放大了短租约对现场抖动的敏感性。
- 从数值上看，Follow 并未发散：第二个 waypoint 的距离在约 2.19 秒内
  从 0.7308 m 连续降至 0.2625 m，已经接近 0.25 m 到达容差。但该段
  ONNX RL 的 boom、stick、swing 多次达到归一化限幅，视觉上会显得
  转动激进；因此“运动门意外关闭”和“倾倒路径观感/目标合理性”是两项
  独立问题，修复租约不能替代现场对路径的人工验收。
- 当前活动 DUMP 目标是 `[-0.2, -0.90, 0.1]`。Git 历史证明它在
  2026-08-22 的“prepare live dump trajectories”提交中由
  `[-0.2, -1.21, 0.1]` 主动调整而来，并非运行时读取了随机旧值。
  若现场期望点不同，必须同时修改并验证 `excavation_cycle.json` 和
  `excavation_demo.json`，不要仅凭画面临时重跑。

**修复**

1. Orin resident Mission lease 默认值由 1500 ms 调整为 3000 ms；
   PC 突然掉电或断网时仍会自动归零，只是最坏检测窗口增加到约 3 秒。
2. 续租 SSH 使用独立的 1 秒命令超时，不再被普通控制 RPC 的 30 秒
   超时拖住；心跳允许 1.5 秒瞬时失败恢复，连续失败仍会 fail closed。
3. WebUI 的“安全停止”可用性改为依据 Mission worker 是否仍存活，
   而不是依据页面是否已经收到 `failed` 阶段；清理中的失败任务仍可停止。

**回归与复测顺序**

- PC：resident lease、hybrid resident/system/session、WebUI 共 101 项通过。
- Orin：resident control/core/sink/ingress/data-link、state sender、remote
  Follow 共 161 项通过，另 41 个子测试通过。
- 真机复测前先确认目标坐标，再在发动机关闭或可立即急停的条件下只做
  一次分阶段 `ACT 挖掘 → RL 倾倒`；验证连续运行超过 3 秒、无
  `MOTION_GATE_CLOSED`，且运动中点击“安全停止”能立即进入归零清理。

### 6.10 2026-08-25：STM32 诊断无数据时远端进程残留

**现象**

- PC 执行 `python scripts/diagnose_stm32_link.py` 后长时间没有输出。
- PC 上按 Ctrl+C 只中断 SSH 客户端，Orin 上的
  `diagnose-stm32-link` 仍在 `do_select` 中等待，并继续独占
  `/dev/ttyTHS1`。

**证据与处置**

- 现场查到远端 PID 仍持有串口；对该 PID 发送 `SIGTERM` 后，
  `fuser -v /dev/ttyTHS1` 恢复为空。
- PC 包装脚本现对 `--duration-s` 做有限数且 `(0, 60]` 的边界校验；
  远端命令使用 `duration + 5 s` 的 GNU `timeout`，PC 本地再设置
  `duration + 10 s` 的第二层截止。无论 STM32 是否出数，诊断都会在
  有界时间内结束并释放远端进程。
- 修复后第一个 10 s 窗口收到 193 个有效帧与 9 个损坏帧；
  紧接的第二个窗口为 194 帧、0 解析失败、19.335 Hz、最大周期
  61.18 ms。本次判定为启动/接触瞬态污染，不是持续的 V3 运行故障。
- 随后零命令浸泡 `episode_0004` 通过：STM32 19.67 Hz，新传感状态
  9.85 Hz，前视/倾倒相机均为 30.22 Hz，所有非零、解析失败、传感失效
  与序列间断计数均为 0。

**以后的标准恢复命令**

```bash
# 先查是否有诊断残留，不要无条件 kill 其他正常 Runtime
ssh jetson16@192.168.50.2 \
  'fuser -v /dev/ttyTHS1 2>&1 || true; pgrep -af "[d]iagnose-stm32-link" || true'

# 只对已确认的残留 PID 发送 TERM，然后复查释放
ssh jetson16@192.168.50.2 'kill -TERM <PID>'
ssh jetson16@192.168.50.2 'fuser -v /dev/ttyTHS1 2>&1 || true'
```

### 6.11 2026-08-25：V3-A 启动后在 ACT ready 处自动失败

**现象**

- 发动机关闭时点击“自动装车 1 铲”，Orin owner 、STM32、ACT
  模型、CUDA warmup 和 ACT worker readiness 全部通过，但还未进入
  `FOLLOW_DIG` 就自动停止。
- WebUI 报 `TypeError: expected string or bytes-like object, got
  'PurePosixPath'`。Orin 随后按 fail-closed 路径停止，终态零已确认，
  owner、ACT worker、串口与相机全部释放。

**根因**

- 生产 `GuidedEpisodeConfig` 将 Orin Python 路径建模为
  `PurePosixPath`。V3-A PC 适配器构造最终 SSH argv 时，唯一漏掉了
  `str()` 序列化，`shlex.join()` 的正则输入因此抛出上述 TypeError。
- 旧测试的 fake Guided config 使用普通字符串，没有覆盖真实配置类型，
  所以没有在上线前捕获该问题。

**修复与回归**

- 仅在 SSH argv 最终边界把 `rl_orin_python` 显式转为字符串；不改变
  Orin 运行时、RL/ACT 策略、运动授权或安全门。
- 新增集成回归，直接加载活动
  `config/guided_episode.pc.json`，把真实 `PurePosixPath` 类型传到最终
  `shlex` 边界。修复前稳定复现原 TypeError，修复后 V3-A system、
  WebUI runtime/app 相关 61 项测试全部通过。

### 6.12 2026-08-25：V3-A owner ready 后控制请求失败

**现象**

- 发动机关闭时点击“自动装车 1 铲”，owner、ACT build、CUDA warmup 和
  worker readiness 均通过，随后在进入 `RL 到挖掘点` 前自动停止。
- WebUI 报 `remote command failed: resident fixed cycle control request
  failed`；终态零得到确认，owner、ACT worker 和硬件资源均释放。

**根因**

- V3-A launcher 已将普通 resident control/ACT socket 放在
  `~/.local/run/excavator-resident-v3a/`，但遗漏了 fixed-cycle control
  socket 的显式传参。Orin 因而监听旧默认路径
  `~/.local/run/excavator-resident/fixed-cycle.sock`，PC 却请求
  `~/.local/run/excavator-resident-v3a/fixed-cycle.sock`。
- PC 原就绪检查只匹配 `RESIDENT_FIXED_CYCLE_READY` 字样，没有核对 owner
  公布的 socket 路径，因此错误地把不兼容的端点组合判为 ready。

**修复与验收**

1. Orin launcher 现在从同一个 `RESIDENT_RUNTIME_ROOT` 派生 control、ACT
   和 fixed-cycle 三个 socket，并显式传给 `orin_state_sender.py`。
2. PC 启动端会核对 owner readiness 中公布的 fixed-cycle control 和 ACT
   socket；任何路径漂移都会在发送运行命令前 fail closed。
3. Orin launcher/state sender/fixed-cycle 相关 88 项测试及 7 个子测试通过；
   PC V3-A system/WebUI 相关 62 项测试通过。
4. 发动机关闭时无法通过机械运动判断阶段。复测应以 WebUI 的“当前阶段”
   显示或阶段高亮为准：看到 `RL 到挖掘点` 后点击“安全停止”，确认无报错、
   终态零被确认且串口/相机全部释放。

### 6.13 2026-08-25：V3-A 安全停止后误报终态零未确认

**现象与证据**

- 发动机关闭时 V3-A 已正常进入 `FOLLOW_DIG`，RL 推理及 20 Hz STM32
  状态持续输出；点击“安全停止”后页面进入 `CANCELLED`。
- owner 已写出 velocity-reference 终态零 `command_seq=866`，但退出时报告
  `resident terminal zero was not acknowledged`。

**根因**

- cancel 会先永久 disarm 并写出终态零。主串口循环随后仍会收到 STM32 的
  严格回显，但旧 core 在 terminal 状态下没有保存该 ACK 证明。
- PC 在收到 `CANCELLED` 后释放 owner；finally 只能再次等待同一 sequence
  的“有效且未超时”回显。如果先前 ACK 已被主循环消费、随后 STM32 将其
  标记超时，就会产生误报。不能简单删除 ACK 门禁或把超时当成功。

**修复与回归**

- `ResidentMotionCore` 现在只在 sequence、mode、四轴零值、valid 和
  non-timeout 全部严格匹配时，持久记住本轮终态零 ACK。
- owner 关闭串口前优先复用这条已验证证明；若主循环尚未收到，仍执行原
  有的有界串口等待；两条路径都没有证据时继续 fail closed。
- core、state sender、fixed-cycle 组合回归 99 项及 13 个子测试通过。

**现场复测结果**

- 修复后于发动机关闭条件再次启动 V3-A，页面正确显示
  `FOLLOW_DIG`；点击“安全停止”后进入 `CANCELLED`。
- owner 记录 `resident terminal zero acknowledged: command_seq=898
  mode=velocity_reference`，无 traceback；ACT worker、`/dev/ttyTHS1`
  与两路相机均释放。V3-A 的“启动→RL 阶段→安全取消→终态零→
  资源释放”可判定通过。

### 6.14 2026-08-25：V3-A 看似已到挖掘点，但一直不切换 ACT

**现象**

- V3-A 正常进入 `FOLLOW_DIG`，但 WebUI 一直显示
  `RL 到挖掘点`，没有进入 `ACT_DIG`。
- 日志每约 2 s 记录 `RL Follow no progress`；安全停止仍然
  正常，终态零 `command_seq=787` 获得 STM32 严格回显。

**已确认证据**

- 该阶段的实时铲尖坐标约为
  `(0.920, 0.231, 0.461)`，目标 `dig_01=(1.0, 0.26, 0.0)`。
  XY 平面误差仅约 0.085 m，但 Z 误差约 0.461 m，所以三维
  欧氏距离约 0.469 m，确实大于端点到达容差 0.25 m。
- `WaypointTracker` 按三维 `math.dist()` 判定到达；在距离未达标
  时不切换 ACT 是正确的 fail-closed 行为，不是阶段事件丢失。
- V3-A candidate 与 AiryLidar 权威 DIG 终点及 0.25 m 容差一致，
  排除了配置拷贝漂移。
- 关键回归差异：旧版现场成功日志是 `waypoint=2/3`、
  `waypoint=3/3`，最后从 0.315 m 收敛到 0.253 m 后成功；
  V3-A 的固定轨迹却始终是 `waypoint=1/1`。V3-A 把“固定
  终点”误实现成“单点轨迹”，使 RL 38D 观测的三点预瞠退化为
  同一终点重复。

**修复**

- 保留 Orin 上的固定终点和 0.25 m 到达判定，不恢复 PC
  轨迹规划。
- Follow Runtime 首次收到真机状态时，将单个固定终点在 Orin
  本地解析为 `当前铲尖点 → 直线中点 → 固定终点`，恢复与
  旧版一致的三点预瞠语义。
- 如果启动时已在终点 0.25 m 内，仍立即完成 Follow，不强制
  额外绕行。

**复测判据**

- 日志应先出现 `RL Follow fixed endpoint expanded locally`，随后的
  Follow 进度应为 `waypoint=2/3` 或 `waypoint=3/3`，不再是 `1/1`。
- 必须看到三维 `distance_m` 收敛至 `<=0.25`，才应进入
  `ACT_DIG`。如果 Z 仍固定在约 0.461 m，且 boom/stick/bucket
  状态不变，则属于机构未响应、低层限位或 FK/传感标定问题；
  此时应安全停止，不能放宽容差或强制切 ACT。

### 6.15 2026-08-25：倾倒动作完成时 heartbeat 误报失败

**现象**

- V3-A 一铲任务的 `RL→ACT` 和 `ACT→RL` 切换均流畅，
  Orin 严格 ACK 指标分别为 202.35 ms 和 203.08 ms。
- RL 顺利到达倾倒点，固定倾倒动作进入 `EXECUTE_DUMP`
  约 7 s 后，WebUI 报 `heartbeat failed: command_failed`，未记录
  最终 `COMPLETED` 状态。
- 清理路径仍安全：owner 退出前获得了 `command_seq=1892`
  的 velocity-reference 终态零 ACK，ACT worker 随后断开。

**根因**

- PC 的 V3-A `status()` 调用实际发送 `heartbeat`，同一请求
  同时承担“读取快照”和“续租”。
- Orin 在倾倒子动作成功后已原子进入 `COMPLETED`。PC 的
  下一次 heartbeat 在终态窗口到达，旧实现把“终态不可续租”
  表达成 `command_failed`，导致 PC 在读取完成回执前误判任务失败。
- 这是终态回执竞态，不是 RL/ACT 切换失败，也不是倾倒
  Follow 未到达。

**修复与不变量**

- 活动任务的 heartbeat 仍只续监督租约，不推进策略阶段。
- `IDLE` heartbeat 仍严格拒绝。
- 已进入 `COMPLETED`/`FAILED`/`CANCELLED` 的 heartbeat 现在只返回
  不可变终态快照；它不续租、不复活 Mission、不重新获取
  任何运动权限。
- Orin 定向契约测试 51 项通过；本地 Orin Runtime 全量
  341 项及 124 个子测试通过。

**现场复测判据**

- 重启 WebUI 后执行一铲。倾倒完成后应看到
  `stage=COMPLETED cycles=1/1`，页面进入“已完成”，不再出现
  `heartbeat failed: command_failed`。
- 如仍失败，保留完整 owner 日志；不应通过延长固定倾倒
  时间或放宽 Follow 容差掩盖错误。

### 6.16 2026-08-25：WebUI 新按钮出现但请求 Not Found

**现象与根因**

- 页面已显示“清空日志”按钮，点击后提示 `Not Found`。
- 静态 HTML/JavaScript 会在每次浏览器请求时从工作树读取，
  所以旧 WebUI 进程也能显示新按钮；FastAPI 路由只在进程
  启动时注册，旧进程不包含新的 `/api/*/logs/clear` 路由。

**处置与验证**

- 确认 Mission 已终止且 Orin 串口/相机无 Owner 后，精确停止旧
  WebUI PID，用原 V3-A commissioning 命令重启。
- 重启后采集与 Mission 两个清空接口均返回 HTTP 200，快照
  `logs=[]`。清空只影响 WebUI 内存显示，不会停止 Mission，
  也不删除磁盘日志或 Experiment Run 证据。

**预防**

- 修改 FastAPI 路由、Supervisor 组合或 Python 后端后必须重启
  WebUI；只有 HTML/CSS/JavaScript 修改才可仅刷新浏览器。

### 6.17 2026-08-25：V3-A 多铲完成后停在倾倒点

**现象与证据**

- 自动装车两铲的挖掘、运转和倾倒均正常，RL/ACT 交接约为
  150–247 ms；页面最终显示 `COMPLETED cycles=2/2`。
- 第一铲倾倒后日志出现 `FOLLOW_DIG cycles=1/2 target=dig_02`，
  第二铲倾倒后却从 `EXECUTE_DUMP cycles=1/2` 直接跳到
  `COMPLETED cycles=2/2`，没有最终一次 `FOLLOW_DIG`。
- 因此挖机停在倾倒点不是 RL 跟踪失败，而是 V3-A 终态状态机
  漏执行了最后返回阶段。WebUI 在终态统一标绿四段，又掩盖了该遗漏。

**根因与修复**

- 旧 `_complete_or_start_next_dig()` 在已完成铲数达到请求铲数时
  直接发布终态；这与采集运行手册中“最后一铲仍返回该铲使用的
  挖点”不一致。
- 现在每次倾倒完成后都进入 `FOLLOW_DIG`：尚有下一铲时前往下一个
  固定挖点；最后一铲时保持当前挖点不变，返回本铲使用的挖点。
- 只有最终 Follow 成功并确认静止后才进入 `COMPLETED`、终态归零并
  释放 owner。已有 RL/ACT 动作权威、轨迹控制器和交接协议均未修改。

**现场复测判据**

- 两铲任务末尾必须依次出现：
  `EXECUTE_DUMP cycles=1/2 target=dig_02` →
  `FOLLOW_DIG cycles=2/2 target=dig_02` →
  `COMPLETED cycles=2/2 target=dig_02`。
- 最终 `FOLLOW_DIG` 期间页面应显示“RL 返回挖点”；到达 `dig_02`
  后四段才全部标记完成，并应记录 velocity-reference 终态零 ACK。

### 6.18 2026-08-25：RL 中间 waypoint 过度收敛导致不必要停顿

**现象与证据**

- V3-A 两铲任务完整结束，最终顺序为
  `FOLLOW_DIG cycles=2/2` → `COMPLETED cycles=2/2`，且终态零已由
  STM32 ACK；本次没有状态机或安全清理故障。
- 4 次 RL/ACT 权限交接的 ACK 端到端延迟为
  `145.8 / 248.2 / 164.0 / 249.7 ms`，明显停顿不是策略冷启动或
  权限切换导致。
- 从倾倒点返回 `dig_02` 时，中间点 `waypoint=2/3` 的距离
  连续约 6 s 停在 `0.4395–0.4388 m`；最后一次返回也在中间点
  `0.4629–0.4542 m` 出现 no-progress，而当前中间点和终点均使用
  `0.25 m` 的同一到达容差。

**诊断与已实施调整**

- Confirmed：中间 waypoint 是引导趋势的通过点，不应与最终
  交接位姿使用同样严格的空间到达条件。当前的精确追点使控制器
  在几乎共线的中点附近停留，延长了 RL 段但不提高最终到位精度。
- 已保持起点/终点 `0.25 m`，并为中间点增加独立的
  `intermediate_waypoint_tolerance_m=0.40 m`。该字段纳入轨迹 SHA，
  旧 V2/旧轨迹未携带新字段时自动回退到统一 `0.25 m`行为。
  首轮真机验收从 `0.40 m` 开始，不直接跳到更大值；若仍在
  `0.40–0.46 m` 稳定平台停滞，再基于新日志评估 `0.45 m`。
- 实现使用两个显式参数，并在 no-progress 日志中输出当前点
  实际采用的 tolerance；不得通过统一放宽
  `waypoint_tolerance_m` 降低挖掘点/
  倾倒点的最终到位精度。

**预期复测**

- 返回段的 `waypoint=2/3` 不应再连续出现多条 no-progress，中点通过
  应更连续。
- `waypoint=3/3` 仍须收敛到 `<=0.25 m` 才能进入 ACT、倾倒或
  `COMPLETED`；不改变终点精度和 fail-closed 行为。

### 6.19 2026-08-25：两级容差生效后仍有中点停顿

**现象与对比**

- 新日志明确显示中间点使用 `tolerance_m=0.4000`，倾倒终点
  仍使用 `tolerance_m=0.2500`，因此两级容差配置和 Runtime 接线
  确认已生效。
- 两铲主链路从约 `79.9 s` 缩短为 `71.7 s`，改善约 `8.2 s`。
  第一次从倾倒点返回挖点从约 `14.8 s` 降至 `6.8 s`，但最终
  返回仍约 `9.9 s`，因此主观观感只是部分改善。
- 新日志的中点 no-progress 仍出现在 `0.453–0.580 m`；四次策略
  交接只有 `147.9–264.2 ms`，不是本次停顿主因。

**根因证据**

- Confirmed：V3-A 当前直接在 Cartesian 空间使用
  `(start + target) / 2` 生成中间点。从倾倒点返回挖点时，该直线
  中点到回转中心的水平半径只有约 `0.57 m`，但起点、终点和
  实际铲尖运动半径约为 `0.90–1.16 m`。
- 实际铲尖到直线中点的距离分别约为 `0.580 m` 和 `0.453 m`，
  因此即使容差放宽到 `0.40 m`，仍可能在回转弧线上无法及时
  判定通过。
- Strongly suspected：停顿的根因是“直线中点与挖掘机的
  回转弧形运动不匹配”，而不是 RL/ACT 冷启动或权限交接。

**已实施修复**

- 未继续放大中点容差。V3-A 本地中间点已改为
  绕 `machine_root_ros` 回转轴的柱坐标/圆弧插值：角度沿最短回转方向
  插值，半径和高度分别插值。
- 同一日志姿态下，估算铲尖到圆弧中点的距离只有
  `0.278 m` 和 `0.346 m`，均可在现有 `0.40 m` 中点容差下连续通过。
- 保持最终 DIG/DUMP 终点 `0.25 m`、策略输出、交接协议和
  fail-closed 边界不变。倾倒终点 `0.31 m` 附近的收敛停顿与中点
  几何是独立问题，不用放宽终点容差掩盖。
- 该修改不引入 RRT*、PC 规划或新的运动权限；仍由 Orin 在每段
  Follow 启动时基于实时铲尖生成确定性三点预瞧。

### 6.20 2026-08-25：V3-A 在 PC 看不到实际轨迹

**现象与风险**

- V3-A 自动闭环运行正常，但 PC 不再启动旧 `live_commissioning` Operator，
  RViz 中看不到 Orin 本地生成的三点轨迹。
- 操作者无法在发动机关闭的首次验收中核对起点、圆弧中点和固定终点，
  不应仅凭配置文件直接通电运行。

**根因**

- V3-A 的固定终点扩展和圆弧中点均在 Orin `EdgeFollowRuntime` 内生成；
  旧控制状态只回传 Mission stage，没有回传控制器实际使用的 waypoint。
- PC 组合根在 V3-A 分支刻意不创建旧 Operator，以避免 PC Behavior Gateway
  与 Orin 本地闭环争夺控制；这也同时移除了 RViz 的只读显示能力。

**修复**

- Orin 严格 `resident_fixed_cycle_control.v2` status 增加只读
  `active_trajectory`：固定 frame、target、实际三点、当前 waypoint 和当前容差。
- PC 将该快照原子写入 `logs/v3a_active_trajectory.json`，现有 ROS Marker
  Publisher 读取后发布 `/localmap/planned_bucket_tip_markers`。
- WebUI 自动 Mission 前启动 `profile:=live_shadow`、
  `motion_authorization:=LOCKED` 的 Operator/RViz；不启动 Orin Edge Gateway，
  不在 PC 规划或发送动作。Follow 结束即删除轨迹文件并清除 Marker。

**现场验收**

1. 保持发动机关闭，页面点“启动 RViz 显示”，确认真机模型、TF 和雷达正常；
2. 再启动自动一铲，进入 Follow 后应出现红色三点轨迹和黄色 waypoint；
3. 核对终点对应所选 DIG/DUMP，圆弧中点位于合理回转侧；
4. 点安全停止，轨迹应清除且终态零必须由 STM32 ACK。

注意：实际三点依赖 Follow 首帧实时 FK，因此只在 Follow 开始后出现。首次检查
必须发动机关闭；RViz 是只读验收工具，不能替代急停或低层限位。

### 6.21 2026-08-25：WebUI 重复启动提示 8088 端口被占用

**现象**

```text
ERROR: [Errno 98] error while attempting to bind on address
('127.0.0.1', 8088): address already in use
```

**根因**

- 上一个 `scripts/run_collection_ui.py` 进程仍在运行，已经监听 8088。
- 这不是 V3-A 策略或 Orin 故障，而是 PC 本地 UI 重复启动。

**手动关闭**

```bash
pgrep -af '[p]ython.*scripts/run_collection_ui.py'
kill -INT <上一条命令显示的PID>
pgrep -af '[p]ython.*scripts/run_collection_ui.py' || true
```

使用 `SIGINT` 让 UI 执行完整清理，不要默认使用 `kill -9`。

**已实施的自动处理**

- UI 启动时会记录严格单实例 PID 文件。
- 若旧 UI 的采集和 Mission 都已终止/空闲，新启动会先发
  `SIGINT`，等待旧 UI 完整退出，再接管 8088。
- 若旧 UI 正在遥操、采集或执行 Mission，自动替换会拒绝，必须先在
  页面点“安全停止”。
- 若 PID 属于其他程序，不会误杀，需人工检查。

### 6.22 2026-08-25：V3-A 到达倾倒位后因轨迹可视化崩溃

**现象**

```text
ValueError: trajectory visualization waypoints are invalid
V3-A failed: resident fixed cycle control request failed
```

- 问题发生在 `FOLLOW_DUMP` 后进入 `EXECUTE_DUMP` 固定倾倒动作时。
- 日志中 ACT→RL 交接为 `203 ms`，RL 已运行到倾倒终点附近，
  不是策略加载或交接超时。

**Confirmed 根因**

- Follow 和 ExecuteDump 都使用 `type=feedback` 事件。
- Follow feedback 包含 `trajectory_waypoints`，固定倾倒 feedback 只包含
  `step_index/step_label/phase/max_error`，本来就没有 waypoint。
- V3-A Runtime 过去只按 `type=feedback` 判断，把 ExecuteDump feedback
  误送入 Follow 可视化解析器。一个只读 RViz 字段异常因此传播到
  唯一 owner 主循环，导致 Mission 归零后退出。

**修复**

- 只在存在活动 Follow 目标时解析轨迹 feedback；ExecuteDump feedback
  不再进入 waypoint 解析。
- 即使 Follow 的只读可视化字段损坏，也只清除 RViz 轨迹并记录
  warning，不中断策略、权限或 STM32 控制闭环。
- 新增真实 FixedAction feedback 和损坏 Follow 可视化两条回归测试。

**复测标准**

1. 自动一铲进入 `EXECUTE_DUMP` 后，日志可继续显示固定倾倒步骤；
2. 不得再出现 `trajectory visualization waypoints are invalid`；
3. 倾倒完成后应进入 `FOLLOW_DIG`，两铲任务最终回到最后一个挖点；
4. 安全停止仍需看到 STM32 `resident terminal zero acknowledged`。

### 6.23 2026-08-25：ACT 挖掘后去 DUMP 的运料轨迹过低

**现象与风险**

- 自动两铲能够完整结束，但 ACT 挖掘结束后转向倾倒位时，
  铲斗很低，有扫到 pool 上沿的真机碰撞风险。
- 在完成新轨迹的 RViz 只读验收前，禁止继续自动装车。

**Confirmed 证据**

- 第一铲 ACT 结束姿态：铲尖 `z=-0.6995 m`；原圆弧中点
  只有 `z=-0.2997 m`。
- 第二铲 ACT 结束姿态：铲尖 `z=-0.6870 m`；原圆弧中点
  只有 `z=-0.2935 m`。
- DUMP 终点为 `(-0.2, -0.9, 0.1)`。原算法对高度也做线性中点，
  导致挖机在铲斗仍然远低于 DUMP 高度时就开始大角度回转。
- 本次策略交接仍在 `200–218 ms`，不是冷启动或交接慢导致。

**最终修复：只抬高 CarryMaterial 圆弧中点**

- 仍使用原来的三点轨迹，不增加额外状态或抬升点：

  ```text
  ACT 结束位 → 圆弧中点 → DUMP 终点
  ```

- 圆弧中点的 `x/y` 仍由原圆弧算法生成；仅在中点高度低于已配置
  DUMP 终点高度时，将中点 `z` 提高到 DUMP 高度。本现场配置下，
  高度下限就是 `z=0.10 m`。
- 第一、第二铲日志中的原中点高度会分别从 `-0.2997 m`、
  `-0.2935 m` 提高到 `0.10 m`。
- 已移除“铲尖低于某高度就强制将 swing 置零”的动作门控；控制器
  始终连续输出 boom、stick、bucket、swing 四轴动作，避免新增约束
  引起停顿、不可达或策略状态不一致。
- 曾评估“先抬升点 + 低于阈值锁 swing”，因其引入额外 waypoint、
  动作门控和状态边界，按现场决策取消。
- DUMP→DIG 不是带料段，保持原有圆弧返回轨迹。

**必须按顺序验收**

1. 发动机关闭，启动 RViz 只读显示。
2. 运行一铲 Mission，进入 `FOLLOW_DUMP` 后检查三点仍为 ACT 结束位、
   圆弧中点、DUMP 终点；中点应位于 `z=0.10 m` 的回转弧上。
3. 日志应出现 `CarryMaterial endpoint expanded as clearance arc`，
   其 `midpoint_floor_z_m=0.1000`。
4. 轨迹与 pool 上沿有明确视觉间隔后，才可以在急停可用、作业区无人
   的条件下做一铲低速真机复测。

### 6.24 2026-08-25：WebUI 单条采集被 200 条 campaign 门禁阻断

**原现象**

```text
[episode-context] task_variant=dig_only soil_reset_block_id=block_01 dig_point_id=dig_01
ValueError: formal collection requires a clean AiryLidar repository
```

原页面虽然一次点击实际上只创建一条 Episode，但会先通过 SSH 读取固定 200 条
campaign 的 `next_expected_slot`，自动填写标签、显示 `0/200` 进度并执行槽位门禁。
任何 inspector、SSH 或整仓 dirty 问题都会阻止这条 Episode，容易被误解为“一次启动
200 条”，也把单条采集与离线实验计划不必要地耦合。

**最终修复**

- WebUI 一次点击只创建一条 Episode，不会自动连续采集。
- 操作者逐条填写 `task_variant / soil_reset_block_id / dig_point_id`；土壤批次允许使用
  规范化的小写下划线标记，例如 `block_01`、`soil_after_rain`。
- 页面移除 200 条总进度、权威下一槽位和累计完成条数；失败、断连或停止后均可独立
  重新开始下一条。
- WebUI 不再调用 campaign inspector，也不以 campaign 结果控制 Collector、Episode 编号
  或运动生命周期。`inspect_collection_campaign.py` 仅保留为离线计划/审计工具。
- 目标来源门禁只检查实际使用的 AiryLidar 目标配置文件相对 HEAD 是否修改，并继续记录
  commit/path/SHA256；与目标配置无关的 RViz、笔记或代码 dirty 不再阻止采集。
- 目标配置文件自身未提交、采集期间发生变化或 `dig_point_id` 不存在时仍在录制前
  fail closed，避免标签与实际 DIG 坐标不一致。

**复测标准**

1. 打开 WebUI，页面只有“本条 Episode 标记”，不出现 `0/200`、slot 或累计进度。
2. 选择手工预定位或直接采集，填写三项标签，点击一次只启动一条 Episode。
3. 结束并人工选择成功/失败/重录后，页面回到可再次启动状态，不自动创建下一条。
4. 修改 AiryLidar 的无关笔记不会阻止采集；修改实际目标配置文件则应在创建 Episode
   前明确拒绝。

### 6.25 2026-08-25：双相机 Episode 首次启动降频

**现象**

- `episode_0006` 被操作者标记为成功，但权威 raw validator 拒绝：

  ```text
  dump camera rate 24.053 Hz is outside [25.000, 35.000] Hz
  ```

- 后续 `episode_0007` 至 `episode_0009` 的两路相机均约为 `30.22 Hz`，
  无相机序号缺口、队列丢帧或磁盘队列丢帧。
- 随后仅遥操 Collector 连续运行约 12 分钟时，通过
  `/healthz` 实测前视约 `30.29 Hz`、倾倒相机约 `10.10 Hz`，
  说明倾倒相机仍存在长时间降频风险。

**判断**

- Episode 启停、人工失败、重录和 Collector 资源释放语义正常。
- `episode_0009` 的 5 个 `stm32_control` gap 与 20 Hz 控制、
  19.65 Hz telemetry 采样相位匹配；在无串口解析失败、写失败、
  无效传感器且 validator 通过时，不视为数据损坏。
- 当前不应直接开始 200 条正式采集；应先固定倾倒相机、
  确认 USB/曝光状态并通过连续小批次验收。

**现场处置与放行标准**

1. 每条 Episode 结束后以 `excavator-il validate <episode_dir>` 结果为技术放行依据，
   人工点击“成功”不能替代技术校验。
2. 人工失败 Episode 可保留为审计证据，但 converter 要求
   `status=complete && success=true`，不会误进训练集。
3. 先做至少 10 条连续小批次；要求每条两路相机都在
   `[25, 35] Hz`，且 queue/disk drop、parse/write failure、sensor invalid 均为 0。
4. 若倾倒相机再次低于 25 Hz，先停止正式计数，检查固定、光照、
   USB 端口/带宽，并考虑在采集允许前增加双相机稳定帧率门禁。

**2026-08-25 22:33 复测**

- 首次诊断时 PID `253451` 的旧 Collector 同时独占 `/dev/video0`、
  `/dev/video2` 和 `/dev/ttyTHS1`；因此两路 `cannot open UVC camera` 是资源占用，
  不是相机损坏。
- 优雅停止残留 Collector 后，连续 30 秒双相机诊断通过：
  front/dump 均为 `640×480×3`、`30.2 Hz`、906 帧，两路为不同物理设备；
  p95 读取延迟分别为 `22.81 ms` / `23.86 ms`，最大延迟为
  `38.86 ms` / `30.60 ms`，亮度与近黑/近白比例均正常。
- 这证明当前相机、USB 映射和 30 Hz 并发采集本身正常；但也暴露
  WebUI/启动会话结束后 Collector 残留会阻断下一次采集，正式长批次前
  必须验收每条 Episode 后三个设备均无 owner。

### 6.26 2026-08-26：WebUI 重启后仍显示旧采集表单

**现象**

- 服务端已加入 `zone_01`～`zone_06`、区域内重复次数和备注字段，但浏览器页面仍只显示
  旧的动作任务、土壤批次和 `dig_point_id` 三项。

**确认方法**

```bash
fuser 8088/tcp
curl -fsS http://127.0.0.1:8088/ \
  | grep -E 'collection-zone-id|dig-repeat-index|operator-note'
```

- 若 `curl` 能看到三个字段，且 8088 进程工作目录是当前 `excavator-il`，说明服务端已是
  新版，旧画面只是浏览器未重新加载的 DOM。

**解决方法**

1. 在页面按 `Ctrl+Shift+R` 强制刷新。
2. 仍未更新时关闭该标签页后重新打开 `http://127.0.0.1:8088/`。
3. 只有 `curl` 也缺少新字段时，才停止 8088 旧进程并重新运行 V3-A WebUI 启动命令。

该问题不会影响已运行的 Collector 或 V3-A 控制主线；不要为此重启 Orin、STM32 或
重新部署模型。

### 6.27 2026-08-26：首批 110 个 Episode 中有 6 个不能进入训练

**快速巡检结果**

- raw 根目录共有 110 个 Episode 目录。
- 104 条为 `status=complete && success=true`，共 13,007 个训练 step。
- `episode_0001` 为失败；`episode_0003`、`episode_0005`、`episode_0007`、
  `episode_0053`、`episode_0057` 为中止。
- `episode_0001` 有 29 次串口解析失败和 33 个 STM32 control sequence gap；两个中止
  Episode 没有 `quality_report.json`，都不能因目录中存在图像而误收进训练集。
- 104 条有效 Episode 的相机/STM32/动作频率稳定，且 parse/write/sensor/queue/disk
  failure 均为 0；抽样 8 条经权威 validator 均通过。

**处理原则**

1. 原始失败/中止目录不删除，保留作采集审计。
2. 转换输入只使用 `complete + success=true + validator passed` 的冻结清单。
3. 本批 block/zone 没有按权威规则维护，筛选时忽略这些标签，不把它们用于论文分组。
4. 开始训练前仍应跑全量 validator；本次 8 条抽样通过只代表快速巡检，不等于全量冻结。

### 6.28 2026-08-28：V3-A 自动装车在 owner ready 前退出

**现象**

- WebUI 点击自动装车后立即失败，日志只有 `RESIDENT_OWNER_PID`，随后提示：

  ```text
  V3-A startup cleanup failed: remote process exited with return code 1
  RuntimeError: v3a-owner exited before the expected readiness signal
  ```

**根因**

- PC 的 V3-A 配置引用 Orin 上的
  `deploy/v3a/field/fixed_cycle.field.json`，但此前只保留了
  `deploy/v3a/candidate/`，正式 `field/` 工件未进入版本化同步范围。
- 启动脚本先打印 PID，随后用无说明的 `test -f` 退出，因此 WebUI 只能看到泛化的
  readiness 错误。该故障发生在打开串口和启动 ACT worker 之前，与 STM32、相机、
  RL/ACT 策略或 CUDA 无关。

**修复**

1. 使用已完成 7 铲的 Experiment Run
   `run_20260827T135403.350184Z_c55316afb6` 生成验证记录，并从原候选轨迹提升出
   `field_validated` 固定循环 plan。
2. Orin 严格 loader 已验证 plan 包含 `dig_01`、`dig_02`、`dig_03` 和 `dump`，
   实际 ACT 上限为 130 steps。
3. PC/Orin 六份 field JSON 的 SHA256 已逐项一致；启动脚本也已同步。
4. 启动器现在会在检查串口前明确报告：

   ```text
   固定循环 plan 不存在：<完整路径>
   ```

5. PC 曾显示 125 steps，但 Orin 候选 plan 才是 V3-A 本地闭环的实际权威，成功的
   7 铲实际使用 130 steps。现已把 PC 显示恢复为 130，避免显示与执行不一致。

**复测标准**

1. 发动机关闭时先启动 1 铲，页面应进入 `RL 到挖掘点`，不再在 readiness 前退出。
2. 点击安全停止，应看到 terminal zero acknowledged，且 owner、ACT worker、串口和相机均释放。
3. 发动机开启后的顺序仍为 1 铲，再根据结果扩到 4 铲；不要把本次工件恢复与
   125-step 参数实验混在同一次验收中。

### 6.29 2026-08-28：V3-A 进入 RL Follow 后报告 status fields invalid

**现象**

- owner、ACT warmup 和硬件门禁均已通过，并已根据实时铲尖位置生成三点圆弧；
  随后 PC 报错并触发安全停止：

  ```text
  ValueError: V3-A status fields are invalid
  ```

**根因**

- PC 的严格状态协议已新增 `mission_profile`，要求 10 个字段；Orin 仍运行旧的
  9 字段 `resident_fixed_cycle_control.v2` 实现。
- 这是 PC/Orin 部署不完整导致的同一 schema 内字段漂移。PC 严格拒绝未知契约是正确的，
  不应通过放宽解析器继续运动。

**修复与验证**

1. 同步固定循环状态机、控制协议和运行时三个强耦合模块，而不是只复制启动脚本。
2. 离线跨端重放修复前稳定复现同一异常；修复后 Orin 返回包含
   `mission_profile=regime_factorized` 的完整 10 字段状态，PC 可严格解析。
3. Orin 对应状态机/控制/运行时测试 54 项通过；PC 与 Orin 三个生产文件 SHA256 一致。
4. 故障发生后 owner 已完成 terminal zero ACK 并释放资源；没有绕过安全门。

**预防**

- 固定循环协议变更必须把生产模块和契约测试作为一个同步集合；不能只同步 launcher、
  plan 或单个 Python 文件。
- 后续提交时应固化 PC/Orin 配对 commit，并在真机启动前执行一次 status 离线交叉解析。

### 6.30 2026-08-28：ACT 切换 RL 仍有肉眼可见停顿

**现象**

- V3-A 自动装车 4 铲完整成功，ACT 常驻、RL 常驻、传感器有效，但每次 ACT 挖掘结束后，
  RL 开始运转前仍能看到短暂停顿。

**证据与结论**

- 本次 owner 日志为
  `resident_fixed_cycle_20260828_135020.v3a-owner.log`，Experiment Run 为
  `run_20260828T055025.203784Z_0e6bb91341`。
- 4 次 ACT→RL 从 ACT 终态零 ACK 到 RL 第一条非零命令 ACK 分别为
  150.7、146.1、197.6、147.6 ms，平均 160.5 ms；没有模型冷启动或 PC 轨迹规划等待。
- RL 第一帧原始输出已经较强，例如 `[-0.72, -1.0, -0.246, 1.0]`；ONNX 推理约
  0.3–2.6 ms，因此不是 RL 模型输出死区速度，也不是推理慢。
- 当前 `follow_action_slew_rate_per_s=3.0`。第一帧发给执行器的归一化命令被限制到约
  `±0.145`，随后约每 50 ms 增加 `0.15`，约 0.35 s 后才接近目标幅值。
- 以 Orin 权威 action audit 的实时铲尖位置计算，从 ACT 终态零 ACK 到铲尖首次移动
  5 mm 分别约 400、405、452、451 ms，平均 427 ms。这是当前肉眼停顿的主要来源：
  **安全交接约 0.16 s + RL 软启动与液压建立运动约 0.27 s**。

**处理建议**

1. 保留 terminal zero ACK、generation/authority 和目标策略 zero claim，不通过跳过安全
   交接来换速度。
2. 在 V3-B 中把“安全交接延迟”和“可见运动延迟”分开记录；正式指标增加终态零 ACK
   到首个越过死区命令、铲尖 5 mm 位移两个时间。
3. 优先只调整 Follow 启动斜率，先做 `3.0 → 4.0 /s` 的单变量发动机关闭测试；稳态和
   反向斜率先保持不变。不要注入固定最小杆量，也不需要因此重训 RL。
4. 发动机关闭完成至少 20 次 ACT→RL 交接后，再按 1 铲、4 铲、7 铲顺序真机复测；
   验收目标为安全交接不退化、可见运动延迟 p95 小于 1 s，并检查液压冲击和轨迹超调。

**2026-08-28 实施状态**

- V3-B 已实现分轴双斜率：首次从零接近非零目标使用 `4.0 /s`，到达首个目标后的正常
  跟随、减速和反向使用 `3.0 /s`。
- 终态零、取消、安全停止、无效状态归零不经过斜率限制，原有 zero ACK 与
  generation/authority 交接不变。
- PC 新增/相关回归 135 项通过，Orin Runtime 全量 296 项通过；代码与活动配置已同步
  到 Orin。尚未进行发动机关闭或带液压 HIL，因此本项仍需按上面的 20 次与
  1/4/7 铲顺序验收。

**2026-08-28 真机斜率 4/6/10 对比**

- 对应 owner 日志分别为
  `resident_fixed_cycle_20260828_143447.v3a-owner.log`、
  `resident_fixed_cycle_20260828_143636.v3a-owner.log` 和
  `resident_fixed_cycle_20260828_143748.v3a-owner.log`。4/s、6/s 各包含一次
  ACT→RL；10/s 包含两次完整 ACT→RL，并在第三铲 ACT 阶段由用户停止。
- 参数确实生效：首条非零 ACK 的 swing 归一化命令约为 0.12、0.16、
  0.289/0.30；命令达到约 0.95 满幅的时间约为 314 ms、254 ms、156/214 ms。
- ACT 终态零 ACK 到铲尖首次位移 5 mm 的时间约为 363 ms、354 ms、
  356/418 ms；到 20 mm 的时间约为 463 ms、404 ms、456/467 ms。
- 因此 6/s 相比 4/s 有小幅实际收益；10/s 明显加快了命令爬升，但没有继续缩短
  机构可见起动时间。首条非零 ACK 后到 5 mm 位移仍稳定约 197--206 ms，说明当前
  下限主要来自液压建立运动、状态采样和安全交接抖动，而不是启动斜率。
- 三组均无串口、generation、authority 或模型冷启动故障。三组都出现倾倒终点
  约 0.30 m 的 `no progress` 警告，这是既有 waypoint 达容差问题，不能归因于斜率。
- 当前样本仅 1、1、2 次 ACT→RL，足以确认命令级差异，但不足以形成正式统计结论。
  工程上优先采用 6/s 作为平滑性与起步速度的折中；10/s 暂不作为默认值，除非后续
  同一起姿重复测试证明其无冲击且可见运动指标更优。
- 对 10/s 的两次交接进一步按可见位移分解：终态零 ACK 后到铲尖 5 mm 为
  356/418 ms，50 mm 为 556/563 ms，100 mm 为 670/681 ms，200 mm 为
  1021/1081 ms。因此“5 mm 可测起动小于 1 秒”和“肉眼感觉约 1 秒甚至略长”并不矛盾；
  人眼通常在机构形成连续、较大位移后才会认为切换完成。ACT 最后一次肉眼运动还可能
  早于协议计时起点，实际主观停顿会比 zero-ACK 指标略长。

### 6.31 2026-08-28：在 Orin 唯一串口执行边界增加异步 action audit

**目的**

- 区分“ACT 末尾已进入固件死区但 step budget 仍未结束”和“ACT 已结束、RL 接管后
  液压尚未形成可见运动”这两类肉眼停顿。
- 本次只修改 Orin Runtime，不修改 STM32 固件，不改变 ACT/RL 动作值、authority、
  generation、zero claim 或 terminal-zero ACK。

**实现边界**

- `ResidentCommandSink` 是 RL/ACT 共用的唯一 STM32 串口写边界。在这个边界记录：
  `command_write`（实际写入）和 `command_ack`（STM32 telemetry 确认）。
- `ResidentMotionCore` 记录每个 `act_step`，并在 ACT step budget 结束时记录
  `act_segment_summary`。摘要包含最后一个至少有一轴越过固件手动杆量死区的 step、
  连续全轴死区 step 数和按 10 Hz 估算的尾段时长。
- 固件手动动作死区的诊断镜像为每轴绝对值 `0.15`；该阈值**只适用于 ACT 的
  `manual_action` 归一化杆量**。`all_axes_in_deadzone=true` 只表示这条 ACT 手动杆量的
  四轴都不会越过固件杆量死区，不会修改或裁剪任何命令；不得用它判断 RL 的
  `velocity_reference` 物理速度是否进入死区。
- JSONL 由 daemon writer thread 异步落盘。控制线程只做内存 record 和非阻塞入队；
  队列满、目录不可写、序列化失败或磁盘失败只丢失证据并计数，不改变串口写入结果，
  关闭时也有有界等待。

**日志位置与检查**

正式 launcher 默认写入：

```text
/home/jetson16/workspace_excavator/excavator-orin-runtime/logs/resident_action_audit.jsonl
```

可查看最近的 ACT 尾段摘要：

```bash
jq -c 'select(.event_type == "act_segment_summary")' \
  /home/jetson16/workspace_excavator/excavator-orin-runtime/logs/resident_action_audit.jsonl \
  | tail -n 10
```

可按同一 `runtime_id`、`generation` 联结 `act_step`、`command_write`、`command_ack`，
再与 owner 的 handoff sample 对齐。第一次真机采样重点比较：

1. `estimated_trailing_deadzone_ms`；
2. ACT terminal-zero ACK 到 RL 第一条非零 ACK；
3. RL 第一条非零 ACK 到铲尖 5/50/100 mm 位移。

只有第 1 项较大时，才说明主要问题是 ACT 尾段太长；若第 1 项很小而第 3 项大，主要
问题仍是液压建立运动/可见位移，而不是策略切换协议。

### 6.32 2026-08-28：执行器审计确认 ACT 固定尾段是主要停顿来源

**现场记录**

- 本次单铲的 owner 日志为
  `resident_fixed_cycle_20260828_194618.v3a-owner.log`，audit `runtime_id` 为
  `1a26848c93634cd2ba763c298b2a6aed`。运行中无模型冷启动、串口写入或
  generation/authority 错误；最后由用户停止，终态零已获 STM32 ACK。
- ACT 共完成 130 步。第 114 步是最后一条有任意轴越过手动杆量死区
  `0.15` 的动作：`[0.195552, -0.000635, 0.003789, 0.0]`，STM32 回显为
  `[0.196, 0, 0, 0]`。
- 第 115--130 步连续 16 步四轴均落入死区；STM32 对每步的实际回显都是
  `[0, 0, 0, 0]`。第 114 步到第 130 步的真实 step 时间为
  1652.0 ms；从第 114 步命令 ACK 到 ACT terminal-source zero ACK 为
  1733.4 ms。
- ACT terminal-source zero ACK 到 RL 第一条非零命令 ACK 只有
  149.8 ms，其中目标模式 zero claim 约 50.1 ms，策略准备等待约
  54.6 ms，首条命令写入到 ACK 约 45.1 ms。首条 RL 命令已经是物理速度参考：
  `[boom_mps=-0.013529, stick_mps=-0.033678, bucket_mps=-0.004801,
  swing_radps=0.582890]`；这些物理量不能与 ACT 杆量死区 `0.15` 作数值比较。
- 因此，从“ACT 最后一条能导致运动的命令 ACK”到“RL 首条非零命令
  ACK”的完整静止窗为 1883.2 ms。约 92% 发生在 ACT 仍未结束的死区尾段，
  只有约 8% 是安全交权。

**结论与后续**

- Confirmed：本次肉眼可见的约 2 秒停顿主因是固定 `act_max_steps=130`
  让 ACT 在动作已全部进入固件死区后继续推理 16 步；不是 ACT/RL 冷启动，
  也不是 generation/authority 切换过慢。
- 不能跳过 terminal-zero ACK、target zero claim 或 generation 门禁；这些安全步骤
  本次只占约 150 ms，不是应优化的主要部分。
- 不建议仅根据一铲盲目将上限固定改为 114/115；不同料堆和起始位姿下，
  ACT 的有效结束步可能变化。
- 下一个小步开发应为：保留硬上限，在足够的最小 ACT 步数之后，当连续
  3--5 个已被 STM32 确认的 ACT 动作四轴全部位于死区时，提前完成
  ACT segment；中间偶发小动作不得触发。需先用更多旧日志/发动机关闭 HIL
  确定最小步数和连续步数，再做 1/4/7 铲真机验收。

### 6.33 2026-08-28：12 铲实测否定固定补偿和简单连续死区结束

**数据完整性**

- 连续执行 3 次 4 铲，对应 audit `runtime_id` 为
  `0cabbc5d291a463eb3a6a382f7786b71`、
  `1d0793ffb41145d99229fe13ff95e9af` 和
  `3fb5c76ff8664cfbbb7675bd45dbed06`。
- 每次都完整记录 4 个 `act_segment_summary` 和 520 个 `act_step`，共 12 个
  ACT segment/1560 个 ACT step；JSONL 无损坏行。每段仍使用
  `max_steps=130`。

**12 铲统计**

- 本节所有“有效 ACT 动作”“全轴死区”和“最后有效步”均只按
  `source=act_dig, mode=manual_action` 的归一化杆量及其 `0.15` 固件死区判定；
  没有把 RL 的物理速度参考套用到该阈值。

- 最后有效步为
  `107, 105, 121, 114, 113, 106, 103, 115, 123, 108, 105, 121`；
  范围 103--123，中位数 110.5，均值 111.75。
- 尾部连续死区步数为
  `23, 25, 9, 16, 17, 24, 27, 15, 7, 22, 25, 9`；
  范围 7--27，中位数 19.5，均值 18.25。
- 最后有效 ACT ACK 到 terminal-source zero ACK 的中位数为 2051.6 ms，
  均值 1906.6 ms，最大 2789.0 ms。
- terminal-source zero ACK 到 RL 首条非零命令 ACK 的中位数为
  152.8 ms，均值 159.8 ms，最大 199.4 ms。再次确认安全交权不是
  主要停顿来源。
- 最后有效 ACT ACK 到 RL 首条非零 ACK 的整体窗口中位数为
  2204.0 ms，均值 2066.4 ms，最大 2988.4 ms。

**新的关键发现**

- 当前 ACT checkpoint 的 `n_action_steps=10`：每次推理的动作块使用 10 步，
  第 1/11/21/.../101/111/121 步为新块起点。
- 3 铲在尾段出现了 7--9 个连续全轴死区步，随后新动作块的首步又恢复
  有效杆量，例如第 112--120 步均为零回显，第 121 步又输出
  boom `0.181`。
- 因此，“连续 3--5 个死区步就结束”会在真实的 12 铲中误切；即使连续
  9 步也不足以不区分 chunk 边界地判定任务结束。
- 12 铲的最后有效步跨度达 20 步；所以固定从 130 中减去一个“补偿步数”
  同样不可靠。若固定改成 110/115，会截断有效动作最晚到达 121--123 步的
  3 铲。

**推荐的 V3-B 结束判定**

- 不再使用单一固定步数或不区分 chunk 的连续死区计数。
- 当当前 10-step chunk 进入全轴死区时，不立即交权；丢弃该 chunk 未用的
  尾部动作，使用最新相机/状态强制刷新一个 ACT chunk。
- 若刷新 chunk 的开头仍在全轴死区，则可以提前完成 ACT segment；若恢复
  有效动作，则继续执行新 chunk。这正好区分“旧 chunk 尾部已衰减”与
  “基于最新观测的 ACT 仍需继续”。
- 按本次 12 铲的已记录边界作静态估算：6 铲可在第 111 步的新 chunk
  首部结束，3 铲可在第 121 步结束，3 铲仍执行到 130 步；平均约可省
  11.75 步/1.18 s，且不会被已知的 7--9 步假终止窗口误切。
- 实现后必须先通过录制 audit 的离线回放回归，再做发动机关闭 HIL，最后按
  1/4/7 铲验收；terminal-zero ACK、zero claim 和 generation/authority 不改。

### 6.34 2026-08-28：ACT 杆量死区与 RL 速度死区必须分开解释

**两条真实执行链**

- ACT 使用 `manual_action`，输出为 `[boom, stick, bucket, swing]` 四维归一化杆量。
  STM32 的 `JoystickServoMap` 对每轴直接应用绝对值 `0.15` 的手动杆量死区，再映射到
  舵机/PWM。因此 action audit 用 `0.15` 判断 ACT 尾段是否还能开阀是正确的。
- RL 网络先输出归一化速度动作。Orin 按 `shared/machine_profile.json` 中每轴、正负方向
  不同的 policy deadzone 处理后，换算为 boom/stick/bucket 的 `m/s` 和 swing 的
  `rad/s`，再以 `velocity_reference` 发给 STM32。
- STM32 对物理速度参考做闭环 PID 与前馈，得到新的归一化 `valve_action`，该阀杆量最后
  才经过同一个 `JoystickServoMap` 的 `0.15` 开阀死区。由于 PID 输出取决于目标速度、
  当前测量速度、方向和各轴参数，RL 的“有效运动边界”不是一个可直接套用到物理速度
  数组上的固定 `0.15`。

**对既有结论的影响**

- 6.32--6.33 的 ACT 尾段结论保持成立：统计输入只包含 `manual_action/act_dig`，所以
  12 铲中 7--27 个尾部死区 step、chunk 边界恢复动作等证据没有混入 RL 速度语义。
- 需要撤回的只有一种错误外推：不能因为首条 RL 物理速度参考某些分量小于 `0.15`，就说
  它们处于 ACT 杆量死区。RL 首条命令 ACK 只证明速度参考已被 STM32 接收，不等于阀已
  越过死区或机构已经运动。
- 后续总停顿分析必须分别记录五个边界：最后有效 ACT 杆量、ACT terminal-zero ACK、
  RL 首条非零物理速度参考 ACK、PID/前馈阀输出首次越过杆量死区、传感器首次检测到实际
  运动。只有这样才能把 ACT 尾段、协议交权、RL 控制器建压和机构响应分别计时。

**12 铲 chunk 边界复核**

- 12 个 segment 的 step 101 都是有效 ACT 杆量，不能在该处结束。
- 6 个 segment 的 step 111 是新 chunk 的首步且四轴均在 ACT 杆量死区内；这 6 个
  chunk 的 step 112--120 均未恢复有效杆量。
- 另外 3 个 segment 在 step 111 仍有效，但 step 121 的新 chunk 首步进入 ACT 杆量
  死区；这 3 个 chunk 的 step 122--130 均未恢复有效杆量。
- 剩余 3 个 segment 的 step 121 新 chunk 首步仍有效，分别持续到 step 121/123，
  因而必须保留 130-step 硬上限继续执行。
- 因此当前数据支持的最小 V3-B 优化是：保留 130-step 硬上限，只在 step 111/121
  的**新 ACT chunk 首步**得到 STM32 零回显后提前完成 segment。按 12 铲静态回放，
  6 铲可少执行 19 步、3 铲少执行 9 步、3 铲不提前，平均减少 11.75 步（约 1.18 s），
  且不会触发已知的 chunk 内 7--9 步假终止窗口。

### 6.35 2026-08-28：80% deadzone chunk 判定会误删有效 ACT 动作

**判定口径**

- “80%”按较合理的 step 口径解释：一个 10-step ACT chunk 中，至少 8 步的四个
  `manual_action` 轴都在机器配置 `deploy.software_deadzone` 的杆量死区内（当前现场
  配置为 `0.15`）。不能按 40 个轴分量中 32 个
  位于死区来判断，因为 stick/swing 等轴在正常单轴动作期间本来就经常为零，该口径
  会把仍含多个有效 boom/bucket 动作的 chunk 错判为完成。

**12 铲离线结果**

- 采用 100% step 判定（10/10 步四轴全在死区），9/12 个 segment 可以安全提前结束，
  其余 3 个继续到 130 步；已记录数据中没有丢失任何有效杆量。若能在执行前检查整个
  新 chunk，可分别跳过 6 个 20-step 尾段和 3 个 10-step 尾段，平均约 12.5 步/
  1.25 s。
- 降为 80% 后，按每个 segment 的首次命中点回放，会有 2/12 个 segment 被错误提前
  结束。`runtime_id=0cabbc.../generation=6` 的 step 101、111、121 所在 chunk 都是
  9/10 步在死区，但唯一有效 boom 杆量分别为 `0.1744`、`0.1678`、`0.1809`；若在
  step 101 或 111 结束，还会连带丢失后续新 chunk 恢复的有效动作。
- `runtime_id=3fb5c7.../generation=8` 的 121--130 chunk 同样是 9/10 步在死区，
  但 step 121 的 boom 杆量为 `0.1597`，仍高于固件 `0.15` 死区并会实际开阀。
- 因此 80%（甚至离散意义上的 90%=9/10）不满足“不能错误丢失任意 chunk”的要求。
  当前 12 铲证据只支持 100% 的全 chunk deadzone 判定；若未来希望放宽阈值，必须引入
  动作幅值积分/任务完成状态等额外证据，而不能只统计零动作比例。

**2026-08-28 实现口径**

- 判定落在 Orin 唯一串口 owner，不修改 STM32。ACT Worker 的
  `resident_policy_candidate.v2` 只在新 chunk 首步携带完整 10×4 动作块，普通步携带
  `action_chunk=null`；owner 独立复核该动作块。只有已执行至少 100 步且完整 10/10
  步的四轴都位于配置死区，才把该 chunk 作为提前完成证据；9/10 明确不会触发。
- 死区阈值启动时从 edge config 指向的
  `machine_profile.deploy.software_deadzone` 读取并严格校验，随后写入异步
  `act_step.manual_deadzone_abs` 审计字段；运行判断中没有硬编码 `0.15`。RL 的
  `velocity_reference` 各轴物理速度死区不参与此判定。
- 命中时只发送该新 chunk 的首条死区动作，余下 9 步不再执行；owner 等这条
  `manual_action` 获得 STM32 ACK 后，继续原有 ACT 终态零 ACK、RL 零占位 ACK 和
  generation/authority 切换。Worker 提供的是可验证动作证据而不是“完成”布尔 hint，
  不能自行交权。
- 当前硬上限仍作为兜底。若 100% 判定未命中，ACT 会继续到配置的 `act_max_steps`；因此
  这项优化不会把 80%/90% 的模糊块误当作结束，也不会修改 RL 的轨迹或启动斜率。

### 6.36 2026-08-28：提前完成 ACT 后误报 `ACT_STEP_BUDGET_MISMATCH`

**现象与证据**

- 真机单铲进入 `ACT_DIG` 后安全停止，UI 报
  `ACT_STEP_BUDGET_MISMATCH`；STM32 已确认最后一条 `manual_action` 零命令 ACK，
  因而这不是失控或模型推理故障。
- 本次 `resident_action_audit.jsonl` 的权威 segment summary 为：
  `completed_steps=121`、`max_steps=130`、
  `completion_reason=deadzone_chunk`、`skipped_budget_steps=9`。
  这证明 100% 死区 chunk 判定按设计生效。
- 根因是 V3-A fixed-cycle Runtime 把所有 ACT 完成统一上报成
  `STEP_BUDGET_REACHED`，旧状态机又要求完成步数必须精确等于 130；新的合法提前完成
  原因没有穿过本地结果 Interface。

**修复口径**

- Orin `ActSegmentSnapshot` 显式携带核心生成的 `completion_reason`。
- Runtime 只把 `step_budget` 映射为 `STEP_BUDGET_REACHED`，把
  `deadzone_chunk` 映射为 `DEADZONE_CHUNK_REACHED`；未知完成原因继续 fail-closed。
- fixed-cycle 对普通预算完成仍要求 `completed_steps == act_max_steps`；只有可信核心明确
  给出 `DEADZONE_CHUNK_REACHED` 时，才接受 `0 < completed_steps < act_max_steps`。
  因而不会把真正的预算错配静默放行。
- 回归覆盖“121/130 合法进入 FOLLOW_DUMP”和“129/130 伪预算完成仍失败”；Orin 全套
  `377 passed, 131 subtests passed`。代码同步后仍需发动机关闭复测一铲，再进行真机运动。

### 6.37 2026-08-28：ACT 几乎未动作却被死区 chunk 误判为完成

**本次运行证据**

- `runtime_id=ff1f617c...` 并没有在第一个 chunk 立即交权。owner 实际执行到第 101 步后，
  才由现有 `100 steps` 时间下限放行死区判断。
- 第 1--3 步只有 stick 杆量约 `-0.164~-0.165` 勉强越过 `0.15` 死区；第 4--101 步
  全部位于死区。segment summary 为 `completed_steps=101`、
  `last_effective_step=3`、`trailing_deadzone_steps=98`、
  `completion_reason=deadzone_chunk`。
- 对照此前 13 个正常铲次：首个 10-step chunk 均有 9--10 个有效步；总有效步最少 99；
  最后有效步最早为 103；101--110 区间至少有 1 个有效步。本次分别只有
  `3 / 3 / 3 / 0`，与正常样本完全分离。

**结论与待实现边界**

- 单纯把 `act_early_completion_min_steps` 从 100 改成 110 不解决根因，只会让同一个全零
  策略多等待约 1 秒，然后在第 111 步再次误判完成。
- 提前完成还必须具备“本段确实做过有效工作”的进度证据。当前历史证据支持的最小条件是：
  `last_effective_step >= 101`，然后才允许后续完整 10-step 死区 chunk 触发提前结束。
- 到硬上限仍不满足上述进度证据时，不能继续把它当成功挖掘并转入 CarryMaterial；应以
  `ACT_INSUFFICIENT_EFFECTIVE_ACTION` 失败并保持终态零。该门禁能区分正常尾段和“模型从
  开始就没有进入有效动作”的假完成。
- 可选的更早诊断是首 chunk 有效步过少（历史正常最少 9/10，本次 3/10），但 14 铲证据
  尚不足以把这一阈值直接做成真机硬门禁；第一版只记录告警，不据此提前终止。

### 6.38 2026-08-29：V3-B 启动报未知参数 `--expected-dig-catalog-sha256`

**现象与安全边界**

- WebUI 启动八点 Mission 后，远端 owner 打印
  `未知参数：--expected-dig-catalog-sha256`，随后以返回码 2 退出，PC 报
  `v3a-owner exited before the expected readiness signal`。
- 故障发生在串口、相机和 ACT Worker 获取硬件之前；当时
  `/dev/ttyTHS1`、`/dev/video0`、`/dev/video2` 均无 Owner，没有产生运动命令。

**根因**

- PC 与 Orin 虽然都在 `zs/v3-b` 且 Git HEAD 同为 `0105fea`，但两端存在不同的未提交
  工作树。PC 已同步 Catalog V4 的调用方，会传源目录 SHA-256；Orin 的
  `scripts/run_resident_mission_runtime.sh` 仍是较早的部分同步版本，尚未解析该参数。
- Orin 同时缺少新版 `orin_state_sender.py`、`edge_runtime/resident_fixed_cycle.py` 和
  `deploy/v3b/catalog/candidate/`。因此只比较分支名和 commit 不足以证明双端运行字节一致。

**修复与验证**

- 仅同步上述三个代码文件和 V3-B candidate 目录；其余 Follow、authority、sink、audit
  等文件经 SHA-256 比较均一致，没有覆盖 Orin 现场的其他改动。
- 同步前的远端文件备份在 `/tmp/v3b-catalog-sync.Bv1joe`，可恢复。
- Orin 本机 `bash -n`、`py_compile`、V4 Catalog artifact SHA 校验通过；定向回归
  `88 passed`。计划为 `resident_fixed_cycle_plan.v4`，源 Catalog SHA-256 为
  `11a51fd4bbf099f4f7b81d37ee0dddabce6e3e12e3b1d1ac5ef1e2563c74212f`，共 8 点。

**以后排查命令**

```bash
# PC
sha256sum excavator-orin-runtime/scripts/run_resident_mission_runtime.sh
rg -n -- '--expected-dig-catalog-sha256' \
  excavator-orin-runtime/scripts/run_resident_mission_runtime.sh

# Orin
ssh jetson16@192.168.50.2 \
  'sha256sum /home/jetson16/workspace_excavator/excavator-orin-runtime/scripts/run_resident_mission_runtime.sh; \
   grep -n -- "--expected-dig-catalog-sha256" \
     /home/jetson16/workspace_excavator/excavator-orin-runtime/scripts/run_resident_mission_runtime.sh'
```

- 同一 commit 但 SHA 不同，说明未提交工作树没有完整同步；不要通过删除摘要参数绕过门禁，
  应同步 owner、plan loader 和 candidate 这一组契约。

### 6.39 2026-08-29：V3-B 最后一铲因低电量人工终止

- 运行 `run_20260829T065450.488453Z_61eec09a35` 已完成 5 个 Catalog 点位循环，
  第 6 铲开始后由操作员因挖掘机电量即将耗尽主动停止。
- 原始 manifest 和日志仍保留 `failure/TRACKING_TIMEOUT` 的当时记录，不回写、不删除；
  论文统计时必须将本次标注为 `operator_aborted_low_battery`，不当作自主系统失败，
  也不当作成功样本。
- 本次只是 V3-B commissioning 工程证据，不能替代后续独立电量、固定尝试数和
  完整 Experiment Run 证据下的正式对比/消融实验。
